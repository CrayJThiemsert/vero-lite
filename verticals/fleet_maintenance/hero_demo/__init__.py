"""Fleet-maintenance View G hero-demo surface (PLAN-0098).

The second governed hero after ``verticals/procurement/hero_demo/``, and a mirror of it
by FUNCTION rather than by shape: the governance moment is captured the same way (real
engine functions, offline, deterministic), but the money beat is the shipped
``fleet_maintenance_economic_impact`` producer rather than a two-sided supplier ledger
fleet has no columns for (PLAN-0098 D-A).

Read-only + offline + DB-free: no mutation, no LLM, no MS-S1, no session. The offline
test is the gate; a live preview is evidence (CLAUDE.md §8).
"""

from __future__ import annotations
