"""Stop-classifier local-model eval (session 56, Cray-directed).

Measures whether an MS-S1 Ollama model can take over the Stop-hook
pause/proceed/dispatch classification from the API-billed Sonnet — using the
hook's REAL system prompt + user-message rendering for full fidelity.
"""
