# STATUS.md rotation archive — 2026 H1 (recent window, base file)

> **Period covered:** 2026-09-04 (session-275) → onward (the RECENT window)
> **Sibling chain (letters ascend with time; the base file holds the RECENT window):** [`2026-h1b-status.md`](2026-h1b-status.md) (2026-05-10 → 2026-06-09) → [`2026-h1c-status.md`](2026-h1c-status.md) → [`2026-h1d-status.md`](2026-h1d-status.md) → [`2026-h1e-status.md`](2026-h1e-status.md) → [`2026-h1f-status.md`](2026-h1f-status.md) → [`2026-h1g-status.md`](2026-h1g-status.md) (2026-07-17 session-142 → 2026-07-24 session-171) → [`2026-h1h-status.md`](2026-h1h-status.md) → [`2026-h1i-status.md`](2026-h1i-status.md) → [`2026-h1-status.md`](2026-h1-status.md) (base, newest — rotations append HERE). The separate `2026-h1-current-focus.md` (sessions ≤46, ratified as-is) is a Current-Focus-only artifact predating this chain.


Rotated out of `docs/STATUS.md` per the **STATUS.md Rotation Policy**
(`docs/runbooks/memory-architecture.md`, Lesson #23). Tier-3: **grep + windowed reads
only, never a whole-file Read.**

**Split lineage.** At session 80 the combined `2026-h1-status.md` first crossed R4's
~192 KB bar and was split into a recent-window file and its `-b` sibling. The recent
window then grew back to **592,577 B — 3.01x the split trigger and 2.26x the 256 KB
cap** — because R4 had no mechanism: its responsibility-matrix guard column read `—`
where R1 and R7 read `fail`. Session 144 added that mechanism
(`tools/check_archive_size.py`, #789) and this file is one of the four it forced.
**No content lost:** every section is preserved verbatim and exactly once across the
chain, verified by exact list equality at split time, not by a byte-sum estimate.

**Second split — session 193.** This file reached **194,232 B**, 2,376 B under the
trigger, with a ~10,661 B Current-Focus block due to rotate in; the sessions-142→171
sections spilled to [`2026-h1g-status.md`](2026-h1g-status.md) and this file kept its
recent window, dropping to **46,215 B**. Arithmetic: 194,232 = 2,129 header + 148,017
spilled + 44,086 retained. **Verified the same way as the first split** — the 65
section headings were listed before and after and compared for exact equality, not
estimated from byte sums.

**Structural note (honest).** R4 describes an archive as TWO sections — rotated
Current Focus blocks and rotated Recent Decisions rows, *newest at top*. That is not
the shape on disk: the file drifted into **one section per reconcile, appended at the
bottom** (27 of them by session 144), and the old preamble's own "Period covered" had
gone stale years of sessions ago. This split preserves the drifted shape rather than
silently rewriting history to match the spec — reconciling R4's text with the real
convention is separate work, deliberately not done here.

---

**Third split — session 247.** This file stood at **189,574 B** with only
**7,034 B** of headroom under R4's ~192 KiB trigger, and the
Active-TODOs compliance trim (R2's ~600-char pointer cap) had **21,958 B** of full
originals to archive — 14,924 B more than would fit. The sessions-173→225 sections
spilled to [`2026-h1h-status.md`](2026-h1h-status.md) and this file kept its recent
window. **Verified the same way as the two earlier splits** — the section headings were
listed before and after and compared for **exact multiset equality**, not estimated from
byte sums, and the `###` blocks (where the post-s229 window actually lives, appended
under the s229 heading rather than as new `##` sections) were counted the same way.

---

**Fourth split — session 297.** This file closed the s295–296 reconcile at
**193,988 B**, only **2,620 B** under R4's 196,608 B (192 KiB) split trigger, while
`docs/STATUS.md` stood at 63,281 B with **2,255 B** of headroom under R1 — so the next
reconcile was forced to rotate, and each of the s291–s294 reconciles had added 3,390–5,538 B
here (s295–296: 1,740 B), more than the room left. The split therefore ran first, as its
own PR. The sessions-226→273 sections
(2026-08-13 → 2026-09-02) spilled to [`2026-h1i-status.md`](2026-h1i-status.md) and
this file kept its recent window from session 275; the boundary sits on a `##` heading.
Arithmetic, read from `git show 9803e22:`: 193,988 = 3,557 header + 136,366 spilled +
54,065 retained. **Verified more strictly than the three earlier splits** — the original
body equals the spilled body, one newline, and the retained body **byte for byte** (that
newline is the trailing blank line the end-of-file hook strips from the new file's last
line), and the `##` / `###` headings were compared as an exact multiset as well.

---

## Rotated at the session-275 reconcile (2026-09-04)

**Recent Decisions rows** — count rule, two entered so two left.

| 2026-08-28 | **s259–260 — ONE PR (#1309, five commits): a 7-phase FDE readiness programme is RATIFIED (Cray) and phases 0/1/1.5/1.6 shipped.** 🔴 **Phase 1.5's *keep `gpt-oss:20b`* verdict is SUPERSEDED** — every challenger "error" was a **deadline cut** (counts matching 3/3, 2/2, 0/0), so that count measured the **harness, not the model**. 🔴 **Turning the reasoning pass off improved BOTH models** (gpt-oss β 10→30%, qwen 30→80%), making it a **pipeline** property, not a model one. ⚠️ **No model is bound** — one repeat per cell against a measured **45% flip rate**. | `7306f17` / [#1309](https://github.com/CrayJThiemsert/vero-lite/pull/1309) / `benchmarks/model_compare/RESULTS-1.6.md` |
| 2026-08-27 | **s258 — ONE PR (#1307): PLAN-0107 COMPLETE 15/15 and ARCHIVED; AC-9 closed on Cray's ruled option (b).** 🔴 The golden corpus was **not an oracle** — invariants 1–4 compare each file to itself, so `tools/golden_trace/` plus a new **invariant 5** now score the system's LIVE composition against the recorded envelope (`created_at` the sole exclusion, measured 1 of 16 keys). 🔴 **AC-12/13/14 had landed unticked while STATUS claimed them closed, and `check_ac_consistency.py` reported clean** — it searches `AC-N` backwards from `CLOSED`. **That guard gap is NOT fixed.** | `d78eebe` / [#1307](https://github.com/CrayJThiemsert/vero-lite/pull/1307) / `docs/plans/done/0107-*.md` |

**Active TODO rows — the first TTL pass.** Cray ruled a **TTL of 25 sessions** (typed, 2026-09-04, s275) with gate rows and rows awaiting a ruling **exempt**. Fourteen rows aged past the bar; **four were kept after reading each one whole** (a pending Cray decision whose wording the exemption regex missed; a narrowed-not-closed gap with no owner; three unresolved s240 items; and one row that says in its own text *"recorded here because nothing else carries it"*). The ten below each name a tracked home in their own text, which is what R2's carve-out requires before a trim. Moved, never dropped (R4).

- [ ] **🆕 ADR-0035 — L1 re-read a SECOND time, and OQ-7 ruled, both Cray-typed s242.** L1 becomes *"one gate at the edge; app code may READ the verdict, never gate itself"*, unblocking phase-2 identity **capture** while **validation** stays pilot-era. **OQ-7 = (b)**: absent edge identity → proceed and **stamp the absence**. ⚠️ Two costs Cray accepted: runs stay unattributed until someone reads the stamps and **nothing alerts on it**; the stamp shape is PLAN-0112's to specify, not the ADR's. **Read the ADR:** `docs/adr/0035-hosting-and-exposure-model.md`.
- [ ] **🆕 PLAN-0110 SD-E is REVERSED (Cray, typed, s242); its commissioned follow-on PLAN-0112 is COMPLETE and ARCHIVED s246.** The original ruling stands as history and is **NOT** edited. ⚠️ **Two consequences no AC owns:** `/runs` filtering is client-side only *because* the population was pinned at two, and the Monitor "all" filter has no cap — SD-6(b)'s bounded default covers Tab H, a **different surface**. **Read the archived PLAN:** `docs/plans/done/0110-fleet-demo-run-markers-filter-and-deploy-reset.md` (§SD-E · §Out of Scope).
- [ ] **🆕 The two s241 pre-commit guards are FLOORS, NOT CEILINGS.** `tools/check_retired_claims.py` (hook #18; convention `docs/conventions/retired-claims.md`) cannot catch a **reworded** stale copy and cannot know a claim *should* have been retired — declaring stays a human act. `tools/check_ac_consistency.py` (hook #19) cannot read a **phase-level** claim (*"Phase A CLOSED 6/6"* names no AC — three exist), and covers **active** PLANs only. ⚠️ `docs/plans/done/0042-at2-managerial-build.md` carries a duplicate `AC-13`, **deliberately out of scope** as frozen history.
- [ ] **The ฿ realized-vs-projected join — REHOMED s235 to `services/db/run_analytics.py::benefit_rollup`,** which is the primitive the circulating framing wrongly claimed could be reused. 🔴 **It cannot** — it yields no per-run figure at all, so the join needs a NEW per-run aggregation; copy `run_duration_totals`. ✅ **No migration needed.** ⚠️ Re-priced **~150–250 lines across 6–7 files, ONE PR** — the *"~40 lines"* figure was checked and is WRONG. Three TEST-PINNED constraints live in that docstring. **Read it, never a restatement.** Lands on Tab J, which fleet publishes.
- [ ] **ADR-0036 OQ-2 — the aggregate in-flight LLM posture — remains OPEN.** ✅ PLAN-0103 Step 9's MS-S1 headroom is MEASURED s221 (`docs/logs/2026-08-10-oct-energy-migration-phase2-and-step9-headroom.md`): RAM and CPU do not constrain a second or third published system. ⚠️ **OQ-2 does NOT follow from it** — the constraint on a second *assisted* system is the resident model and concurrent in-flight calls, not container footprint, and one term (Postgres idle) is declared unmeasured rather than folded silently into the total. **Read the ADR's own OQ-2.**
- [ ] **PLAN-0100's residuals outlive the PLAN** (COMPLETE 13/13, ARCHIVED s216; the demo is LIVE, REDEPLOYABLE and DRIVEN). ✅ **REHOMED s235 under the R2 carve-out** — all three residuals (D-4 discharged by PLAN-0104 · no cache-purge step and no versioned font URLs · no `OLLAMA_KEEP_ALIVE` pin, so all three profiles inherit the 30m code default) now live in the PLAN's own **§Post-archival amendment**, each re-verified at source. **Read it:** `docs/plans/done/0100-exposure-published-demo-surface.md`.
- [ ] **PLAN-0096 residuals — ANSWERED s189; nothing blocking remains.** **Open, NON-blocking:** cost-center (ศูนย์ต้นทุน) granularity — per truck or per company? Ship the column, fill the rule when it lands. **RR-1** per-฿ approver→case attribution is INFERENCE, not data (wrong the day two approvers share a gate resolution). **`latest_per`** still collapses two open cases on one truck (**Cray typed (ค) defer** — the older reports as *ungoverned*). (A5 **parked**.) _[RR-1 + `latest_per` carried here s250 from In-Flight; detail in `docs/plans/done/0096-fleet-flow-completion-phase1.md`.]_
- [ ] **Rock 4 — s84 deep research DELIVERED → O-sequence locked.** Cray locked **O-1 → O-3 → O-2 → O-4**. **O-1** (Box-4 ฿ pitch) **DONE** · **O-3 = ADR-0025 Accepted** · **O-4 = PARK** (agent-interop; `docs/adr/0032-*.md` D4 — option-only, un-park = a counterparty's *written* pull). **Remaining: O-2 only** (economic-impact facet + Q3 data-binding = Rock 3). Full detail: `docs/adr/0025-at2-managerial-layer.md` (O-sequence + Box-3 fit + the **evidence-asymmetry** finding rehomed s142). *(s84 Cray ask)*
- [ ] **PLAN-004 Phase C — OPTIONAL POLISH (forward-declared; "may never land"):** HTML/markdown handoff dashboard under `docs/` + references-graph (mermaid dispatch chains) + `render_transcript.py` unified session export (PLAN-0004 §"Phase C"). *(Phase A + B both COMPLETE — session 35; the prior TODO's validator **warning-swallow bug was FIXED #312**, s58.)* _[s226: the three never-formally-scoped sub-ideas are ROTATED to `docs/status-archive/2026-h1-status.md` — fold them in only if Phase C ever lands.]_
- [ ] **PLAN-0102's two residues outlive the PLAN** (retire L1 loop-detect — COMPLETE 11/11, ARCHIVED s217; L1 gone from all four hooks, L2/L3/L4 intact and asserted so). ✅ **REHOMED s235 under the R2 carve-out** — the **callerless `observe()`** (kept deliberately; deleting it pulls a refactor into every surviving increment) and the **forwards-call-graph method debt** (`ruff` flags a dead import, never a dead private function) now live in the PLAN's own **§Post-archival amendment**. **Read it:** `docs/plans/done/0102-retire-l1-loop-detect.md`.

### Rotated at the s276-277 reconcile — the session-261 Recent-Decisions row [on the R2 count rule alone: one s276-277 row entered a table already at its cap of 10, so the oldest left. Caller-measured at 748 B. Its substance keeps the homes the row itself names — `benchmarks/model_compare/RESULTS-1.6.md` for the think=False measurement and the five-cell matrix — read off the row, not re-grepped here, so `asserted-not-verified`. The s254 and s255 rotation-ledger entries are deliberately NOT re-appended: the Recent-Decisions ledger was archived whole and PRE-PRUNE at the s267 reconcile, which the caller re-measured before writing (a narrow substring probe read 0 and was a FALSE NEGATIVE; a widened probe with a positive control found the s254 note under its own `###` header and the s255 row under the s268 one).]

| 2026-08-29 | **s261 — FOUR PRs (#1310–#1313): two of three phase-1.6 audit findings CLOSED; the third's model is on the box, unrun.** 🔴 **`think=False` does NOT turn gpt-oss thinking off — MEASURED** (a **3,105-char** trace on a live 1-item run), so 1.6's `gptoss/full` and `gptoss/think_off` were **one request**: the §4 *"p95 anomaly"* is two runs of one config, and the next matrix has **FIVE cells, not six**. 🔴 `num_predict` was unset, so a deadline **discarded every token produced** — bounded now. 🔴 The shell-hygiene advisory fired on **the idiom it prescribes** (30.8% of 950 commands); FP 3→0. | `41c0d4c` / [#1313](https://github.com/CrayJThiemsert/vero-lite/pull/1313) / `benchmarks/model_compare/RESULTS-1.6.md` |

### Rotated at the s278 reconcile — the session-263 Recent-Decisions row [on the R2 count rule alone: the s278 row entered a table already at its cap of 10, so the oldest left. Caller-measured at 829 B, verified byte-identical to `git show HEAD:` before carving. Its substance keeps the home the row itself names — `benchmarks/model_compare/RESULTS-1.6.md` for the stage-2c matrix and the procedure-goal defect — read off the row, not re-grepped here, so `asserted-not-verified`. No `[x]` TODO row rotated: the section still holds zero completed rows.]

| 2026-08-30 | **s263 — ONE PR (#1321, five commits): stage 2c completed the five-cell matrix, and the matrix then found a defect in OUR OWN procedure.** 🔴 **The `fleet` procedure GOAL told the LLM to check a gate that is evaluated deterministically downstream with no LLM**, withholding its threshold — live on **11 of 14** items, the rule fires on **one**. Rewritten (`0a1061f`, runtime spine), verified on two models: qwen q8 β/α **85.7 → 100%**, consistency **12/14 → 14/14**; gpt-oss identical, its 100% being **compliance-by-omission** (gate named in **1 of 17** items vs **17 of 17**). 🔴 **~60% of the q4 handler gap was COMPRESSION.** 🔴 `fleet` is at **ceiling for both models**. | `43f707a` / [#1321](https://github.com/CrayJThiemsert/vero-lite/pull/1321) / `benchmarks/model_compare/RESULTS-1.6.md` |
| 2026-08-31 | **s264 — TWO PRs (#1323, #1324): the `fleet` ceiling is broken on a FOURTH axis, offline, with zero MS-S1 runs.** Scoring the `rationale` from the six dumps already on disk separates the tied models in **every cell, no overlap** — qwen **4–8/14**, gpt-oss **0–1/14**. 🔴 **The load-bearing result is NEGATIVE:** `names_amount` does not separate them and gpt-oss scores *higher*, so the intuitive "state the amount" rule would rank them backwards. ✅ **Bar RULED (Cray, typed): role-naming alone** — the richer criteria rest on facts the ontology lacks, making a higher bar an **ontology move before a grader move**. ✅ §12's *blocking-prerequisite* claim **RETIRED** (superseded, not an error). | `9cf5549` / [#1323](https://github.com/CrayJThiemsert/vero-lite/pull/1323) / [#1324](https://github.com/CrayJThiemsert/vero-lite/pull/1324) / `benchmarks/model_compare/RESULTS-1.6.md` §13 |

### Rotated at the s280 reconcile — the session-264 Recent-Decisions row, one completed Active-TODO row, and three Recent-Decisions rotation-ledger entries [the row on the R2 count rule alone: the s280 row entered a table already at its cap of ten, so the oldest left; caller-measured 927 B. The TODO row under the R2 completed-row carve-out (PLAN-0121, executed end to end at s279); 917 B. The three ledger entries (s269-273, s274-275, s276-277) fall outside the new 278/279/280 window under the Cray s267 ruling; 2,795 B. 🔴 Sibling warning for anyone probing this file later: it already holds a DIFFERENT session-264 row, archived at the s279 reconcile, so a present-once probe must run at about 120 characters — a needle short enough to stop at the date and session number matches the sibling and reads as a false duplicate. All payloads carved from `git show HEAD:docs/STATUS.md`; present-once here and absent-from-STATUS were verified separately, each against a positive control.]

| 2026-08-31 | **s264 — THREE more PRs (#1325, #1326, #1327): the axis becomes a shipped lane, and STATUS gets its headroom back.** ✅ **B2** puts the rationale check in `grader.py` as a **fourth lane with a consumer at every step**, `goal` keyword-only + `None`-default so **no goal ⇒ no check ⇒ β/α byte-identical** — the pre-existing 268-test suite passing untouched is that evidence; lane isolation probed in **both** directions. ✅ **STATUS 65,452 → 63,203 B** (headroom 84 → 2,333) via one asserted transaction, archives verified by byte **DELTA**. ✅ Lessons **#0051** (a saturated benchmark may be missing an AXIS, not needing harder data) + **#0052** (a criterion may only demand what the run supplies) authored. | `3cd1609` / [#1326](https://github.com/CrayJThiemsert/vero-lite/pull/1326) / [#1327](https://github.com/CrayJThiemsert/vero-lite/pull/1327) / `benchmarks/procedure_baseline/grader.py` |

- [x] **✅ PLAN-0121 (a DB-contended probe child must not report `GREEN`) — EXECUTED end to end at s279, Steps 0–5, ledger 8 of 8, 16 probes WITNESSED.** All four SDs were RULED (a) (Cray, typed, s277) and none needed reopening — SD-1's stated condition (`None` must stay legal for the five injected runners) was checked on disk, not assumed, and did not fire. §2's recursion hazard held: the parent judge is never probed by its own battery, and the out-of-band oracle in `tests/tools/test_probe_battery_contention.py` covers the direction the battery cannot see. **`Status:` is still `Draft` — the closeout `git mv` to `docs/plans/done/` has NOT run**; check the tracking-stub / supersession-pointer conventions before moving it. Two batteries are committed under `tests/batteries/plan-0121-*.json` and the PLAN carries its `**Batteries:**` line, so Check 3 is satisfied. **Read:** `docs/plans/0121-*.md`.

The oldest row (**s257**) rotated to the same file at THIS (s269-273) reconcile — one row entered, so one left to hold the table at ten, on the **count rule alone**; caller-measured **767 B**. Its substance keeps the home the row itself names — `docs/plans/done/0107-*.md` — **read off the row, not re-grepped here**, so `asserted-not-verified`. Also rotated under the R2 completed-row carve-out: **two `[x]` TODO rows** — the three s268 findings (all closed; homed at PLAN-0118 AC-1(d)/SD-6 and `DECISION.md` §5a) and the s270 Thai-prose ruling (homed at `DECISION.md` §5-RULED). ✅ **Caller-measured:** archive **+3,671 B** (767 + 1,190 + 990 B + header), all three byte-identical to `git show HEAD:`, present-once / absent-from-STATUS verified separately. **Follow-up (#1366):** the completed guards TODO row rotated to the base under the carve-out — **637 B**, archive **+1,172 B**, byte-identical, present-once / absent-from-STATUS. **TWO rows rotated to the same file at THIS (s274-275) reconcile** — **s258** and **s259–260** — because **two** entered (one per session) and the table holds ten, on the **count rule alone**; neither is a cap overage. Their substance keeps the homes the rows themselves name — `docs/plans/done/0107-*.md` for PLAN-0107 and `benchmarks/model_compare/RESULTS-1.6.md` for the phase-1.5/1.6 verdicts — **read off the rows, not re-grepped here**, so `asserted-not-verified`. **No `[x]` TODO row rotated: the section holds ZERO completed rows**, so the R2 carve-out had nothing to take. ⚠️ No byte delta measured — no shell; the caller owes `wc -c` + append + verify-by-DELTA. The oldest row (**s261**) rotated to the same file at THIS (s276-277) reconcile — one row entered, so one left to hold the table at ten, on the **count rule alone**; caller-measured **748 B**. Its substance keeps the home the row itself names — `benchmarks/model_compare/RESULTS-1.6.md` — **read off the row, not re-grepped here**, so `asserted-not-verified`. **No `[x]` TODO row rotated: the section still holds ZERO completed rows.** 🔴 **This ledger's OWN s267 + s268 entries are NOT re-appended — a claim corrected on measurement.** It first recorded them as written *after* s267's whole-and-PRE-PRUNE archive and therefore unarchived. Measured false: the s254 note sits in `2026-h1-status.md` under its own `###` header and the s255 row under the s268 one. ⚠️ **A narrow substring probe read 0 on both and was a FALSE NEGATIVE** — the archive rewrites a rotation note into its own header format, so only a widened probe carrying a positive control found them. ✅ **Caller-measured:** base archive **147,703 → 149,281 B (+1,578)**, the row byte-identical to `git show HEAD:`, present-once and absent-from-STATUS verified separately.

### Rotated at the s281 reconcile — the session-265-266 Recent-Decisions row and the s278 Recent-Decisions rotation-ledger entry [the row on the R2 count rule alone: the s281 row entered a table already at its cap of ten, so the oldest left; caller-measured 869 B. The ledger entry (289 B) falls outside the new 279/280/281 window under the Cray s267 ruling and was probed absent here before appending, against a positive control. No completed `[x]` TODO row rotated: the Active TODOs section holds zero. Both payloads carved from `git show HEAD:docs/STATUS.md`.]

| 2026-08-31 | **s265-266 — TWELVE PRs (#1329-#1340): PLAN-0117 EXECUTED and its experiment MEASURED.** `Vendor` 3 to 12 properties in two ruled bands; all 9 ACs closed, 16 of 16 WITNESSED. The AFTER run on MS-S1 (typed §8 go) shows the unlock **usable** (supplier band gpt-oss 1 of 3, qwen 3 of 3) and **zero harm** — `fl-01`..`fl-10` identical to BEFORE case-for-case on both models despite a +56% prompt. 🔴 The s265 handoff's `fl-03` discriminator claim is **RETIRED by measurement** (`truck_class` gained no synonyms). 🔴 **OPEN:** whether `fl-21` and `fl-22` should score the answer or the query shape. | `docs/plans/done/0117-fleet-ontology-supplier-evaluation-facts.md` · `benchmarks/nl_query_feasibility/RESULTS.md` · [#1335](https://github.com/CrayJThiemsert/vero-lite/pull/1335) · [#1337](https://github.com/CrayJThiemsert/vero-lite/pull/1337) |

The oldest row (**s263**) rotated to the same file at THIS (s278) reconcile on the **count rule alone**; caller-measured. Its substance keeps the home the row itself names — `benchmarks/model_compare/RESULTS-1.6.md` — read off the row, not re-grepped here, so `asserted-not-verified`.

### Rotated at the s282 reconcile — the oldest Recent-Decisions row (s267, #1343-#1346), on the count rule alone: one row entered, so one left to hold the table at ten. Caller-measured 838 B, carved from `git show HEAD:docs/STATUS.md`. Its substance keeps the home the row itself names, read off the row and not re-grepped here, so `asserted-not-verified`.

| 2026-09-01 | **s267 — FOUR PRs (#1343–#1346): two published numbers were measuring the apparatus.** 🔴 **A p95 below 20 samples IS the sample maximum** — nearest-rank `ceil(0.95n)` = `n` for `n < 20`, so the n=14 procedure bar and the NL lane published maxima under a percentile's name in **every run ever made**; fixed at the reporting layer (`tail_s`/`tail_label`), 4 call sites, NL half previously untested. 🔴 **`think_off` costs rationale quality** — qwen q4 `fleet`: `full` 7/14 · `think_off` 4/14 · `skip` 5/14, control-gated; `names_amount` still **inverts** the ratified bar, now inside one model. Batteries 11/11 WITNESSED. | `8843000` / [#1344](https://github.com/CrayJThiemsert/vero-lite/pull/1344) / [#1346](https://github.com/CrayJThiemsert/vero-lite/pull/1346) / `benchmarks/model_compare/RESULTS-1.6.md` |

## Rotated this reconcile — sessions 283 + 284 (2026-09-07), Recent Decisions 10-row window + rotation ledger

### Rotated at the s284 reconcile — the oldest Recent-Decisions row (s267 tail + s268, #1348–#1355), on the count rule alone [one row entered, so one left to hold the table at ten. 🔴 **Probed with a 120-character needle against a KNOWN SIBLING**: this file already carries a different `| 2026-09-01 |` row (`s267 — FOUR PRs (#1343–#1346)`), and a short needle stopping at the date and session number would have matched it and read as a false duplicate. Count 0 at 120 chars, against a positive control (`| 2026-08-20 |`, count 1) and a fabricated needle (count 0). The Recent-Decisions rotation-ledger entry below was probed **separately** — the two ledgers' entries have not shared a fate before.]

| 2026-09-01 | **s267 tail + s268 — EIGHT PRs (#1348–#1355): three are one lesson — a claim is only as good as the surface its consumer reads.** 🔴 **The `llm_assist` gate advisory is DO-NOT-WIRE** — the model's whole input is `" ".join(reasons)`, already on the approver's screen, and `detail.narrative` has **one writer, zero readers**; #1349's rubric, ruled that morning, scored a position #1352 cut that night. Offline, **§8 go UNUSED**. ✅ **PLAN-0118 drafted (#1353), five SDs RULED (#1354), Steps 1–2 shipped (#1355)** — 11 intake cases, 8 scored. 🔴 The Step-2 authoring check was **vacuous by construction** (8 of 8 → honest **4 of 8**). | `a0b743b` / [#1352](https://github.com/CrayJThiemsert/vero-lite/pull/1352) / [#1355](https://github.com/CrayJThiemsert/vero-lite/pull/1355) / `benchmarks/model_compare/DECISION.md` §5a-RESULT |

The oldest row (**s264**) rotated to `2026-h1-status.md` at THIS (s280) reconcile — one row entered, so one left to hold the table at ten, on the **count rule alone**; its substance keeps the home the row itself names (`benchmarks/procedure_baseline/grader.py`), **read off the row, not re-grepped here**, so `asserted-not-verified`. Also rotated under the R2 completed-row carve-out: **one `[x]` TODO row** (PLAN-0121, complete at s279, homed at `docs/plans/done/0121-*.md` since its s282 archive). 🔴 **All three were probed SEPARATELY and all three appended** — each absent (count 0) against three positive controls, one of them proving the method finds a known-archived ledger entry, so the s278 FALSE-NEGATIVE shape did not recur. ✅ **Caller-measured:** base archive **151,590 → 157,226 B (+5,636)** — s264 row 927 B, the completed PLAN-0121 TODO row 917 B, the three ledger entries 2,795 B; present-once verified by **DELTA** (pre=0 post=1 each), absence-from-STATUS separate. 🔴 **The s264 row has a SIBLING already archived at s279** — probe this file at ~120 characters; a 21-character needle matches the sibling and reads as a false duplicate.

## Rotated this reconcile — session 285 (2026-09-08), Recent Decisions 10-row window + rotation ledger + one completed Active TODO

### Rotated at the s285 reconcile — the oldest Recent-Decisions row (s269–273, #1357–#1370), on the count rule alone [one row entered, so one left to hold the table at ten.]

| 2026-09-03 | **s269–273 — TWELVE PRs (#1357–#1370): PLAN-0118 COMPLETE 6/6 and archived; the empty-body failure is MEASURED as the `num_predict` cap, and it is the CALL PATH, not the model.** 45 of 45 empty attempts carry `done_reason=length` with `eval_count` 1024 across three arms, and both Qwen arms are **worse** than `gpt-oss` (74% / 75% vs 53%). 🔴 **Three of Code's own claims were withdrawn on re-measurement** (#1370) — `eval_count` was never broken, the arithmetic was Code's; the reasoning-ate-the-budget mechanism is unsupported; a second-segment budget is CONTESTED and open. ✅ The three subagent write guards are live and witnessed (#1362/#1363), marked `was an error` (#1366). | `8ac17f5` / [#1369](https://github.com/CrayJThiemsert/vero-lite/pull/1369) / `docs/plans/done/0118-intake-extraction-benchmark.md` · `benchmarks/intake_extraction/RESULTS.md` · Lesson #0057 · ADR-0018 §Amendment |

### Rotated at the s285 reconcile — the completed PLAN-0120 Active TODO row [ticked `- [x]` at s285; the PLAN closed 11 of 11 and was archived to `docs/plans/done/` in #1424/#1425, so the row leaves the live list. Its full closure records live in the archived PLAN, which the row itself points at.]

- [x] **✅ PLAN-0120 (goal-gate test-database isolation) — `Complete`, AC ledger **11 of 11**, ARCHIVED to `docs/plans/done/` (s285, #1424).** Steps 5 + 7 closed the last three: **AC-9** through the real seam (a contended probe-battery child classifies `ABORTED` and credits nothing; battery `plan-0120-ac9-battery-child.json`, 3/3 WITNESSED, GAPS 0), **AC-10** at true CI scope (seven exit codes `0`; `4951 passed, 8 skipped`; token `outcome=ACQUIRED … db_tests=490`), **AC-1's live WSLENV half** (`{"C1": "pass"}`, controlled three ways). 🔴 **AC-9's text was reconciled to the code, not merely ticked** — its mutation subject named a file with **zero** `pytest.exit(` sites, and its pass read `{NO-TESTS, GREEN}` became `superseded by new info` when PLAN-0121 landed `Outcome.ABORTED`. ⚠️ Two deviations recorded on their ACs: `node.exe` over WSL interop (no `node` in WSL), and `run_goal_gate` driven directly rather than `stop_continuation.main` — whose classifier arm has contacted MS-S1 since 2026-06-12, so Step 7's "MS-S1 is not involved" was false when written — classified **`was an error`**, not `superseded by new info` (Cray, typed, s285). Generalisation recorded on Step 7: a §8 "not a host-state action" claim must name the **component**, never the containing hook. **Read:** `docs/plans/done/0120-*.md`.

🔴 **THIS (s281) reconcile rotates the oldest row (s265-266)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Its substance keeps the home the row itself names (`docs/plans/done/0117-*.md` · `benchmarks/nl_query_feasibility/RESULTS.md`), read off the row and not re-grepped here, so `asserted-not-verified`. **No `[x]` TODO row rotated — the section holds none.** 🔴 **This ledger's own s278 entry was probed absent and appended**; the Current-Focus ledger's s278 entry was probed and found ALREADY archived, so it was NOT re-appended — the two ledgers' entries did not share a fate, which is why each is probed separately rather than as one blob. ✅ **Caller-measured:** base archive **157,226 → 158,953 B (+1,727)** for the s265-266 row (869 B) and that ledger entry (289 B), both carved from `git show HEAD:`, present-once by **DELTA** (pre=0 post=1 each) with the 157,226 B prefix proven byte-identical afterwards. **No `[x]` TODO row rotated: the section holds zero.**

## Rotated this reconcile — session 286 (2026-09-08), Recent Decisions 10-row window + rotation ledger

### Rotated at the s286 reconcile — the oldest Recent-Decisions row (s274, #1371–#1374), on the count rule alone [one row entered, so one left to hold the table at ten. No `[x]` TODO row rotated — the section holds zero, grep-verified.]

| 2026-09-03 | **s274 — FOUR PRs (#1371–#1374): PLAN-0119 drafted and all NINE SDs RULED (Cray, typed), with three factual defects corrected.** 🔴 **The *proposed* PLAN-0117 closeout was audited by four specialists and its premise found WRONG** — the work had shipped at s265-266 and only the checkboxes were unticked, while several ACs were structurally incapable of failing. Cray ruled a **full re-witness** rather than a tick. ✅ STATUS hygiene (#1374) homed the `Counterparty` deferral and corrected two Active-TODO rows that were measurably false. | `51591d2` / [#1373](https://github.com/CrayJThiemsert/vero-lite/pull/1373) / [#1374](https://github.com/CrayJThiemsert/vero-lite/pull/1374) / `docs/plans/0119-*.md` |

🔴 **THIS (s282) reconcile rotates the oldest row (s267, #1343–#1346)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Its substance keeps the home the row itself names (`benchmarks/model_compare/RESULTS-1.6.md`), read off the row and **not** re-grepped here, so `asserted-not-verified`. ⚠️ **PROBE BEFORE APPENDING with a ~120-character needle** — `s267` text already appears in the archive (ledger entries through s267 are archived) and the live `s267 tail + s268` row is a same-date sibling, so a short needle reads as a false duplicate; the s264 row hit exactly this shape at s280. **No `[x]` TODO row rotated — the section holds none.** ✅ **This ledger's own entries are all in-window (280, 281, 282) — nothing rotates out of it.** ⚠️ **No byte delta measured — no shell; the caller owes `wc -c` + append + verify-by-DELTA.**

### Rotated at the s287 reconcile — the oldest Recent-Decisions row (s275, #1375–#1379), on the count rule alone [one row entered, so one left to hold the table at ten. Carved from `git show HEAD:docs/STATUS.md`, not from the scribe's return. No `[x]` TODO row rotated — the section holds zero, grep-verified.]

| 2026-09-03 | **s275 — FIVE PRs (#1375–#1379): PLAN-0117 CLOSED, but only after eight AC definitions (C1–C8) were repaired — several could not fail by construction. 16 of 16 WITNESSED** (`claims: 86 · RED: 15 · exempted: 71 · GAPS: 0`, + AC-1(a) by hand). 🔴 **The goal gate fabricated nine test failures — the THIRD time (s228/s253/s275):** a `check` `pytest` criterion bound the same per-checkout test DB as a running suite; the serialized re-run of the byte-identical command gave **4801 passed, 0 failed**. ✅ **ADR-0018 D8** (resource-binding contract) + four latent harness defects fixed (#1376). ✅ **PLAN-0120** drafted, six SDs RULED (a). | `dfe7fca` / [#1375](https://github.com/CrayJThiemsert/vero-lite/pull/1375) / [#1377](https://github.com/CrayJThiemsert/vero-lite/pull/1377) / `docs/plans/done/0117-*.md` · `docs/adr/0018-*.md` §D8 |

### Rotated at the s288 reconcile — the oldest Recent-Decisions row (s276–s277, #1380–#1391), on the count rule alone [one row entered, so one left to hold the table at ten. Its substance keeps the homes the row itself names (`docs/plans/done/0120-*.md` · the #1383 / #1389 PRs). The row measures **837 B**; ⚠️ an `awk length($0)` reading of the SAME row said **821**, because `length()` counts *characters* and the row's en-dashes and em-dashes are multi-byte UTF-8 — R1 gates on bytes, so the character figure does not merely round, it measures the wrong quantity. 🔴 **Also rotated: the Recent-Decisions rotation-ledger's own s284 entry** (**770 B**). It had no row left to travel with — its row was archived at s284 — and was held only by the ledger's own window; that ledger had drifted to FOUR entries at s287 against a header declaring a window of three, so dropping the oldest holds it at four (285, 286, 287, 288) rather than letting it reach five. Probed SEPARATELY from the Current-Focus ledger's entries and found ABSENT from this archive against a positive control that found known archived content (`s275 — FIVE PRs` = 1). No `[x]` TODO row rotated — the section holds zero, grep-verified. Carved from `git show HEAD:docs/STATUS.md`, not from the scribe's return.]

| 2026-09-04 | **s276–s277 — TWELVE PRs (#1380–#1391): PLAN-0120 Steps 0–4 shipped, and Step 0 overturned two of the PLAN's own drafted premises** (F10 wrong in both directions; AC-9's pass read named an outcome the classifier never produces). One pytest per checkout is now **ENFORCED** — a second arriver exits **75** naming the holder. 🔴 **The AC ledger is still 0/11 ticked while nine ACs' work is on `main`** — the same shape as the s274 PLAN-0117 row, so a repeat, not a one-off. ✅ **PLAN-0121** drafted, four SDs RULED (a). Three real bugs fixed, two in same-session code. Batteries: 74 claims, **32 RED**, 0 gaps; CI green on all 9 heads. | `8859c27` / [#1383](https://github.com/CrayJThiemsert/vero-lite/pull/1383) / [#1389](https://github.com/CrayJThiemsert/vero-lite/pull/1389) / `docs/plans/done/0120-*.md` |

🔴 **THIS (s284) reconcile rotates the oldest row (s267 tail + s268, #1348–#1355)** to `2026-h1-status.md` (**160,152 B**) on the **count rule alone** — one row entered, so one left to hold the table at ten. Its substance keeps the home the row itself names (`benchmarks/model_compare/DECISION.md` §5a-RESULT), read off the row and **not** re-grepped here, so `asserted-not-verified`. **No `[x]` TODO row rotated — the section holds none.** 🔴 **This ledger's own s280 entry rotates too, with the Current-Focus window (281, 282, 283-284) — probe it SEPARATELY from the CF ledger's s280 entry**: the two ledgers' entries have not shared a fate before (s281). ⚠️ **No byte delta measured — no shell; the caller owes `wc -c` + append + verify-by-DELTA.**

### Rotated at the s290 reconcile — the TWO oldest Recent-Decisions rows (s279, #1399–#1402; s278, #1392–#1398), on the **count rule** [two rows entered (s289, s290), so two left to hold the table at ten. Each probed with a ~120-character needle — a short one stops at the date and session number and matches a same-session sibling — against a positive control DERIVED from the previous reconcile's own diff rather than typed: the first hand-typed control dropped a `#` from `(#1380–#1391)`, read 0, and looked exactly like a missing archive. Rows carved from `git show HEAD:docs/STATUS.md`, not from the scribe's return. No `[x]` TODO row rotated — the section holds zero, measured. ⚠️ **The Recent-Decisions rotation ledger was left UNTOUCHED and has no s290 entry:** it is a single 4,050 B line whose header declares `Window = 283-284, 285, 286 — three` while its entries are 285–288, so it was already drifted on arrival; appending without dropping would deepen that, and surgery inside one 4 KB line was judged the wrong move at the tail of a long session. Registered here rather than papered over (CLAUDE.md §8); the next reconcile owes it a pass. ✅ **Caller-measured:** STATUS **63,704 → 58,204 B**.]

| 2026-09-05 | **s279 — FOUR PRs (#1399–#1402): PLAN-0121 executed end to end — a cut-off pytest child now reports `ABORTED`, not `GREEN`, naming rc, the child's last line and which of two layers decided. 8 of 8 ACs WITNESSED (16 probes, `GAPS: 0`); suite 4876 → 4901.** 🔴 **Six PLAN premises fell to measurement, none to review** — chiefly AC-1's probe **P1 could not redden**, fixed by distinct lead clauses + a conjunction read + AC-5's P9. ✅ **Cray ruled, typed:** `CLAUDE.md` §7 beats the harness's `Co-Authored-By` instruction — no trailer; AI assistance in the commit body. | `af0eca0` / [#1400](https://github.com/CrayJThiemsert/vero-lite/pull/1400) / [#1402](https://github.com/CrayJThiemsert/vero-lite/pull/1402) / `docs/plans/done/0121-probe-battery-contention-legibility.md` (archived s282) · `tests/batteries/plan-0121-*.json` |

| 2026-09-04 | **s278 — SEVEN PRs (#1392–#1398): the "9 of 11 closed" claim did NOT survive audit (seven earned), and committing the battery definitions then exposed a gap in s278's own ticks.** 🔴 **AC-1**'s test witnessed the marker *arriving*, not that it *changes the database*; its **probe 1b had never run**. **AC-2**'s claim was an **exemption**. ✅ Cray ruled **route (ก)** — write the tests the ACs specify; probes 1b/1c/2 WITNESSED, **ledger 0 → 8 of 11**. 🔴 The definitions lived only in `/tmp` though Step 5 demanded otherwise **citing the previous skip**; committed to `tests/batteries/`, which immediately showed AC-7/AC-8 sitting outside every denominator. ✅ **Check 3** now joins PLAN to battery mechanically; R6 + #0047 amended. | `095c419` / [#1395](https://github.com/CrayJThiemsert/vero-lite/pull/1395) / [#1397](https://github.com/CrayJThiemsert/vero-lite/pull/1397) / `tests/batteries/README.md` |

### Rotated at the s291 reconcile — Recent-Decisions row s280 [on the 10-row count rule alone: the s291 row entered the table at the top. The s289 and s290 reconciles rotated no RD row and appended no RD-ledger entry — recorded in STATUS's RD ledger at this reconcile as `was an error`, not silently carried.]

| 2026-09-06 | **s280 — FOUR PRs (#1404–#1407): PLAN-0122 drafted, ratified and Steps 0+1 merged; the Stop hook's proceed arm is ~57% defective over 47 days, counted for the first time.** Two harness defects closed WITH controls — a label leak (`leak_pre=79 leak_post=0`) and a transport divergence (`pre=75 post=3`) — lifting the same model, nothing else changed, from 16/49 to **42/49, 0 unsafe, 49/49 delivered**. ⚠️ **IN-SAMPLE** (one pass, temp 0, tuned on the same 49 cases). 🔴 **SD-1 is resolved by ENTAILMENT, not typed — Step 2 is BLOCKED**, and Step 2.3's `.claude/hooks/*` edit is expected to hit G20. | `7138cc0` / [#1406](https://github.com/CrayJThiemsert/vero-lite/pull/1406) / [#1407](https://github.com/CrayJThiemsert/vero-lite/pull/1407) / `docs/plans/0122-*.md` · `benchmarks/stop_classifier/s280/` |

### Rotated at the s291 reconcile — the Next-Steps immediate-action blockquote [replaced by dispatch 2 of the s291 reconcile: it still named PLAN-0123 Step 1 as the immediate action after Step 1 merged (#1459), and repeated the claim that hook edits are classifier-gated, which s291 measured absent. Its substance survives in the Active-TODO rows for PLAN-0122 and the TODO split; the paragraph is archived so nothing is deleted without a record. Carved from `git show HEAD:docs/STATUS.md`, not from the scribe's return.]

> **Immediate next action is CODE — PLAN-0123 Step 1** (gate repairs + `basis-moved`): every SD is ruled, Steps 1–5 unblocked. ⚠️ Hook edits are classifier-gated and non-deterministic — the PLAN budgets that as its own item with a fallback. **Then:** a fresh **≥ 48 h** sample under the retry shipped s290 (#1449/#1450), before proposing AC-12's new window — which opens only on AC-12's three-part condition. ✅ **SD-8 RULED ≥ 20** (Cray, typed, s290) — the minimum proceed-fire count that window must project to, so the gate is now mechanical rather than a judgement call. The old 88.9%-of-180 transport read is **superseded by new info** — right arithmetic over the lossy field it had; the full `reason` over 187 records splits 62 HTTP 500 · 27 real timeouts · 2 network failures · 74 HTTP-200 with empty `message.content`. Still true: **no `tools/stop_classifier_ledger.py` off the log** — AC-12's input is main-session transcripts. 🔴 **Live specimen, s290:** the Stop hook injected a fabricated instruction whose `reason` invented a user question never asked — **SD-3 / AC-12 reason-quality**, a different defect from the transport one just repaired. ⚠️ `.claude/autonomy-triggers.md` is fed verbatim into the live prompt — any edit voids the measured score and owes its own re-measurement. 🔴 **The Active-TODO split is the structural fix for this file's size** — Cray ruled the WHETHER at s276, the HOW is deferred; Active TODOs is **~34 KB of a ~58 KB file** (Code-measured at s290; stated to the kilobyte on purpose — a byte-exact figure here rots on the very next edit to this paragraph). The items below are the long-horizon register; none of them gates it.

### Rotated at the s292 reconcile — the PLAN-0123 Active-TODO row and the Next-Steps blockquote [both REWRITTEN rather than closed, so this append is belt-and-braces under R4 rather than owed: every fact in the old text was verified re-homed before removal (`docs/plans/0123-*.md` §9 for the five SD rulings, the templates and the two gate defects; PR #1459 + git history for the Step-1 detail; the `wsl.exe --exec bash` consequence is retained verbatim in the replacement row). 🔴 **Two Active-TODO rows were deliberately NOT trimmed, on the R2 carve-out:** the PLAN-0125 rows, because that draft is still UNTRACKED on disk so its facts live nowhere in git; and item ④ of the PLAN-0122 row, because its reach figure (`44.2`) occurs in exactly one file in `docs/` — `docs/STATUS.md` itself. Trimming either would delete a fact from the repository, which R4 forbids; both trim safely once the PLAN-0125 draft is committed and the fabricated-`reason` specimens land in PLAN-0122 §11. Presence below is asserted as a COUNT (want 1), absence from STATUS separately (want 0).]

- [ ] ✅ **Step 1 MERGED s291 (#1459, `b179825`)** — AC-1/2/3 RED-first with printed values; three batteries 8/8 WITNESSED, COVERAGE COMPLETE ×3; `basis-moved` is the eighth check state; the "hook edits are classifier-gated" risk Step 1 budgeted was **measured absent** (no PreToolUse hook is scoped to `.claude/hooks/`; PLAN-0122 G20 had it FALSE at s281 — `was an error` in the Step text). Steps 2–5 unblocked. ⚠️ **Step 2's reason measured live:** the Step-1 goal declared `python -m pytest` cmds read `C1=C2=C3=fail` because the Stop hook runs Windows-side (`C:\Python313\python.exe: No module named pytest`); re-declared with the hook's bridge shape (`wsl.exe --exec bash <script>`) it read `passed` on its own. **🆕 PLAN-0123 (goal declaration — trigger, rendering contract, three templates) — DRAFTED s289 (#1447) and ALL FIVE SDs RULED (Cray, typed, s289): SD-1=(a) PostToolUse-Bash · SD-2=(c) `basis-moved` state · SD-3=(b) three templates, T-GUARD struck · SD-4=(b) `enforce: true` · SD-5=(c) ADR text owed later.** 🔴 **Two source-verified findings reshaped it:** the PostToolUse-Bash hook ALREADY EXISTS, so the nudge needs no `settings.json` change; and under `enforce: false` the warn tier **never reaches the agent's context**. Detail + every ruling: `docs/plans/0123-*.md` §9. Steps 1–5 unblocked. Original direction (typed, s288):** Two halves, together: **(1)** an agent-visible nudge when an ad-hoc *reading* is about to be believed with no live goal, and **(2)** ready-made `/goal` templates for measurement work — (1) alone invites a goal declared just to clear the gate. A Fable design returned four templates (T-COUNT / T-ABSENT / T-ORACLE / T-GUARD) covering 6 of s288's ten instrument errors, and **REFUTED the Stop-hook siting** on four measured points — chiefly that a Stop `systemMessage` reaches Cray's UI only, never the agent's context, and that Stop fires *after* the reading was believed. It proposes **PostToolUse-Bash** instead. 🔴 **Two live defects in the gate itself:** a zero-criteria goal PASSES at the first Stop (`all([])`), and `.claude/commands/goal.md` documents a **600 s** budget that is really `DEFAULT_CHECK_BUDGET_S = 120`. Ten open questions need a PLAN ruling.

> **Immediate next action is CRAY — PLAN-0125 §8 (SD-1…SD-9), especially SD-5 and SD-6, which depart from the typed ②+③+④ shape.** Then CODE commits the draft via a `docs/*` PR. In parallel, CODE: PLAN-0123 Step 2 (the renderer — its reason was measured live in s291); AC-12's window only after `tools/hook_copies_audit.py` reads one hash. ✅ Step 1 merged (#1459). The line this replaces claimed hook edits were classifier-gated — `was an error` (measured absent s291).

### Rotated at the s293 reconcile — the oldest Recent-Decisions row (s281, #1408–#1410), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **811 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from this archive with a 120-character needle against a positive control derived from the s291 reconcile's own diff (=1). 🔴 **Two Recent-Decisions rotation-ledger entries left the window (292, 293) and were DROPPED, not appended — each is already archived in rewritten form:** s288's in the `### Rotated at the s288 reconcile` header above, save its `170,136 → 173,050 B` archive delta, which `f36901c` itself records; s291's in the s291 and s290 headers above (the s280 row, the count rule, the `was an error`).]

| 2026-09-06 | **s281 — THREE PRs (#1408–#1410): PLAN-0122 Steps 2+3 — SLIM5 shipped, then REFUTED on 30 HELD-OUT cases and REVERTED (Cray, typed).** **FULL 29/30, 0 unsafe** vs **SLIM5 28/30, 2 unsafe** — both misses the dangerous way, one a destructive DB op. 🔴 **AC-7 failed two of its three conjuncts and is NOT ticked.** The 42/49 headline was **IN-SAMPLE** on the 49 tuned cases and **inverted** out of sample; FULL's 29/30 is its first honest score. The Stop arm runs the **incumbent** prompt — KEPT: harness repair, sha-pinned constant, `event` seam, plus a new guard test. | `fc01cd0` / [#1409](https://github.com/CrayJThiemsert/vero-lite/pull/1409) / [#1410](https://github.com/CrayJThiemsert/vero-lite/pull/1410) / `docs/plans/0122-*.md` · `benchmarks/stop_classifier/s280/RESULTS.md` |

### Rotated at the s293 reconcile — the PLAN-0123 Active-TODO row and the Next-Steps blockquote [both REWRITTEN when PLAN-0123 Step 3 closed at 3.1 on a measured FAIL (AC-9 FAILED at `p=27.1 %`, AC-10 struck, AC-11 unreachable — `docs/plans/0123-*.md` §11.1), so this append is belt-and-braces under R4, as the s292 reconcile did for their predecessors.]

- [ ] **🆕 PLAN-0123 (goal declaration) — Steps 1–2 COMPLETE; Steps 3, 4, 5 REMAIN.** Step 1 #1459 (`b179825`, AC-1/2/3); Step 2 #1461 + #1463 — **AC-4…AC-8 closed, 5 batteries, 41/41 WITNESSED, `PROBE-COVERAGE: COMPLETE` on each (s292)**. 🔴 **Step 3 = the trigger: AC-9's offline replay FIRST; AC-10's hook only if that replay passes its pre-committed kill criterion.** Step 4 = the field ledger (AC-11, AC-12); Step 5 = closeout. All five SDs ruled (Cray, typed, s289). ⚠️ A `/goal` here needs the hook's bridge shape (`wsl.exe --exec bash <script>`) — `python -m pytest` cmds read `fail` because the Stop hook runs Windows-side (measured s291). ⚠️ `was an error` — Step 1's "hook edits are classifier-gated" risk measured ABSENT (s291). **Read:** `docs/plans/0123-*.md` §9.

> **Immediate next action is CRAY — PLAN-0125 §8 (SD-1…SD-9), especially SD-5 and SD-6, which depart from the typed ②+③+④ shape; then the three s292 classifier items in Active TODOs (the fabricated-`reason` specimens, SD-5's shadow checks, the `CLAUDE.md` §8 line).** Then CODE commits the PLAN-0125 draft via a `docs/*` PR. In parallel, CODE takes **PLAN-0123 Step 3** — AC-9's offline replay first; AC-10's hook only if that replay passes its pre-committed kill criterion. AC-12's window: see the s292 Current-Focus block — its precondition was measured a misread (`was an error`), so it opens without touching the 19 worktrees. Cray's ruling.

### Rotated at the s294 reconcile — the oldest Recent-Decisions row (s282, #1412–#1416), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **916 B** as carved from `git show HEAD:docs/STATUS.md` (reconcile run in s295), probed ABSENT from every `-status` letter with a 120-character needle against a positive control derived from the s293 reconcile's own diff (=1). 🔴 **The RD ledger's s292 entry left the window (293, 294) and was DROPPED — a cross-reference:** its measured tokens (`811 B`, `1,067 B`, `989 B`, `count=1`) each read 1 inside `2026-h1e-current-focus.md`'s `### Rotated at the s292 reconcile` header, which is that entry rewritten; this archive's lone `811 B` hit is the s281 row's size, a different fact. Its "with a positive control" clause is the one residue that header does not restate.]

| 2026-09-06 | **s282 — FIVE PRs (#1412–#1416): PLAN-0121 archived, an R8 unregistered-corpus guard shipped (Lesson #0060), and PLAN-0122's AC-8/9/10 artifacts ALL landed with NOT ONE AC ticked.** 🔴 AC-10's audit: `worktrees=19 · classifier=6 · stop-hook=8` distinct hashes, **not one on main's bytes**. 🔴 **#1416 corrected #1415** — the SD-4 log was built from a pass read, not §4.3. 🔴 Its first production lines exposed **286 of 1,756 MS-S1 `/api/chat` 500s since 2026-08-28**; an offline audit then **CLEARED** s281's held-out run. ✅ **Cray, typed:** worktrees = record-as-abandoned; MS-S1 = change nothing, collect 14 days; registry repair = ONE PLAN after AC-12. | `22fc98f` / [#1414](https://github.com/CrayJThiemsert/vero-lite/pull/1414) / [#1416](https://github.com/CrayJThiemsert/vero-lite/pull/1416) / `docs/plans/0122-*.md` · `tools/hook_copies_audit.py` · `docs/lessons/0060-*.md` |

### Rotated at the s294 reconcile — the PLAN-0109 and fabricated-specimens Active-TODO rows and the Next-Steps blockquote [all three REWRITTEN at this reconcile, so this append is belt-and-braces under R4, as the s292 and s293 reconciles did for their predecessors: PLAN-0109's four ruled-content defects were fixed by #1468 and its Phases 1–2 merged (#1469, #1470); the specimens row's "4 of 6 … in 4 days" was corrected to FIVE across two classifier logs that share their first 256 lines (s294, `was an error`); the blockquote's immediate action moved from Cray's PLAN-0125 §8 to Code's PLAN-0109 closeout. Carved from `git show HEAD:docs/STATUS.md`, not from the scribe's return.]

- [ ] **🆕 PLAN-0109 (Ask over repair cases) carries FOUR factual defects in its RULED content — (i)–(iii) measured s241, (iv) measured s260; zero tracked hits before this reconcile. Fix all four BEFORE Step 5.** ⚠️ **`was an error`, corrected s288:** this read *"`docs/plans/` writes, so G2-gated, owed to a drafter dispatch"* — **measured false against the gate code.** G2 (`pretooluse_governance_gate_deny.py:171-175`) fires only on a Write **creating** a numbered artifact that does not yet exist; G1 (`:184-192`) matches `docs/adr/` **only**. PLAN-0109 exists, so **Code may edit it directly — no dispatch is owed** and this work was never gated. 🔴 **(i) AC-11 would write a FALSE sentence by deleting a TRUE one** — case text reaches the **phrase prompt only**, so its grep pass read **cannot fail**: it is satisfied by deleting a truth. 🔴 **(ii) `tenant_id` is missing from the exclusion enumeration and reddens AC-3 on the first run.** **(iii)** AC-3(iii) contradicts AC-10 on `seq`. 🔴 **(iv) MEASURED s260 — AC-11's pass read is HALF-VACUOUS, and the PLAN's own non-vacuity note is wrong about it.** Running the AC's verbatim command at baseline returns **one** hit, not the *"occurrences in **both** artifacts"* the PLAN asserts: the ROPA contributes one, and `services/db/repair_case_retention.py` contributes **zero** — its docstring **line-wraps** the phrase (`does not reach case` / `text`), so an exact-phrase grep cannot see it. A wrap-tolerant control finds the sentence in **both** files, so it really is there; the AC simply cannot observe the module's copy, and that half of the pass read is satisfied by changing nothing. ⚠️ **The 45-degree fix is the wrong one** — widening the grep would make the AC pass on the ROPA edit alone; the phrase in the module has to become greppable, or the AC needs a per-artifact read. **Read:** `docs/plans/0109-*.md`.

- [ ] **🆕 CRAY'S CALL — record the four fabricated-`reason` specimens in PLAN-0122's AC-12 / §11.** The Stop-hook classifier emitted `proceed`→`block` verdicts whose `reason` invented a user request never made: `2026-09-08T05:06`, `09-09T17:04`, `09-09T23:11`, `09-10T10:56` — **4 of 6 block emissions in 4 days**. Not gated for Code (an Accepted-PLAN edit passes the G1/G2 classifier), so what is owed is a WRITE plus Cray's go, not an unblocking. **Read:** `docs/plans/0122-*.md` AC-12 / §11.

> **Immediate next action is CRAY — PLAN-0125 §8 (SD-1…SD-9), especially SD-5 and SD-6, which depart from the typed ②+③+④ shape; then the three s292 classifier items in Active TODOs (the fabricated-`reason` specimens, SD-5's shadow checks, the `CLAUDE.md` §8 line).** Then CODE commits the PLAN-0125 draft via a `docs/*` PR. **PLAN-0123 Step 3 closed at 3.1 on a measured FAIL (s293)** — Code's remaining PLAN-0123 work is Step 4's AC-12 read and the Step 5 closeout. AC-12's window: see the s292 Current-Focus block — its precondition was measured a misread (`was an error`), so it opens without touching the 19 worktrees. Cray's ruling.

### Rotated at the s295-296 reconcile — the oldest Recent-Decisions row (s283 + s284, #1418–#1422), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **805 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT with a 120-character needle against a positive control **derived from this archive itself** rather than hand-typed (=1) and a fabricated needle (=0) — a typed control dropped a `#` at s290 and read 0, looking exactly like a missing archive. Its substance keeps the homes the row itself names, read off the row and not re-grepped here, so `asserted-not-verified`. 🔴 **This file closes the reconcile at about 193 KB, roughly 3.5 KB under R4's 192 KiB split trigger — the next reconcile that rotates a row owes the continuation file FIRST.** No `[x]` TODO row rotated: the PLAN-0109 row was ticked at this reconcile and leaves at the next one under the R2 carve-out.]

| 2026-09-07 | **s283 + s284 — FIVE PRs merged (#1418–#1422): PLAN-0122 goes 6/12 → 10/12** — AC-8/9/10 (s283), AC-1/2/3 + AC-11 (s284), each with a §11 closeout record; AC-11's clean `check_ac_consistency` carried a positive control printing exactly the 5 predicted gaps. 🔴 A stale PLAN Status line claiming the live Stop arm runs SLIM5 was **false since s281** — refuted in code (`_sonnet_classifier.py:1009-1010`). 🔴 **AC-12's window has n=0**: 48 log lines, `proceed` 0, 39 transport failures — the blocker is DATA, not the missing ledger tool. **Cray's call.** | `006ffd0` / [#1421](https://github.com/CrayJThiemsert/vero-lite/pull/1421) / [#1422](https://github.com/CrayJThiemsert/vero-lite/pull/1422) / `docs/plans/0122-*.md` §4.3 · §11.3/§11.4 · `docs/lessons/0061-*.md` |

### Rotated at the s297 reconcile — the oldest Recent-Decisions row (s285, #1424, #1425), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **645 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter with a 120-character needle against a control derived from this file's own last row (=1). This file was re-based at s297 (#1476): it holds session 275 onward, and `2026-h1i-status.md` holds s226–s273. ⚠️ **The Recent-Decisions ledger's s293 and s294 entries left the window and were DROPPED:** their measured tokens read inside this file's own `### Rotated at the s293 reconcile` / `### Rotated at the s294 reconcile` headers, save s293's note that s288's entry sat outside the `(291, 292)` window its header named — `1794bc2`'s `docs/STATUS.md` records it. The s293 entry had been declared DROPPED at the s295–296 reconcile and left in place (`was an error`).]

| 2026-09-08 | **s285 (#1424, #1425) — PLAN-0120 CLOSED 11 of 11 and ARCHIVED; two of the three ACs it closed were reconciled to the code, not merely ticked.** ✅ AC-9 through the real seam (a contended battery child classifies `ABORTED`, GAPS 0), AC-10 at true CI scope (**4951 passed, 8 skipped**, its stale baseline corrected). 🔴 **SD-3 ruled `was an error` (Cray, typed):** Step 7's *"MS-S1 is not involved"* was false **when written**. MS-S1 was NOT contacted. | `0cef7ed` / [#1424](https://github.com/CrayJThiemsert/vero-lite/pull/1424) / [#1425](https://github.com/CrayJThiemsert/vero-lite/pull/1425) / `docs/plans/done/0120-*.md` |

### Rotated at the s297 reconcile — two completed Active-TODO rows and the Next-Steps blockquote [the `.claude/worktrees/` row leaves under the R2 completed-row carve-out: its one residue was discharged when PLAN-0122 §11.2 (`3a3497d`, s283) proved to BE the AC-10 closeout record it asked for (✎ s295). The PLAN-0109 row leaves because the PLAN is CLOSED 13 of 14 and archived (#1474); its open Cray items are NOT dropped — they continue in STATUS as a new `[ ]` residue row pointing at `docs/plans/done/0109-*.md` §Closeout. The blockquote was REWRITTEN — its immediate action, the continuation file, landed as #1476 — so this copy is belt-and-braces under R4, as the s292–s294 reconciles did. Carved from `git show HEAD:docs/STATUS.md`.]

- [x] **`.claude/worktrees/` is 1.8 GB — 🔴 RULED (Cray, typed, s282): option (1), RECORD AS ABANDONED, LEAVE IN PLACE.** Nothing is deleted and **no worktree file is touched**; SD-6 is audit-only. Measured s256 by set difference: **19 on disk · 7 registered · 6 prunable · 12 UNREGISTERED**; re-measured s282 by `tools/hook_copies_audit.py` (#1414): **`worktrees=19 · classifier hashes=6 · stop-hook hashes=8`, and NOT ONE of the 19 is on main's bytes** (main's version exists in a single copy). ⚠️ `git worktree list` sees only **6 of 19** — UNC gitdirs read `prunable` — so any enumeration must go by **filesystem**, never by git. 🔴 **What remains is a WRITE, not a decision:** the 19-name list + this disposition belong in PLAN-0122's **AC-10 closeout record**, still unwritten. **Read:** `tools/hook_copies_audit.py`. ✎ s295 — `superseded by new info`: PLAN-0122 §11.2 (`3a3497d`, s283) IS that AC-10 closeout record — the 19 names plus Cray's disposition, AC-10 `[x]`. "Still unwritten" was true when written (`60c72dd`, s282); **nothing remains.**

- [x] **PLAN-0109 (Ask over repair cases) — CLOSED s296 (#1474): 13 of 14 ACs ticked on evidence re-measured against `main`, PLAN archived to `docs/plans/done/`.** AC-5's golden oracle now carries a battery (2 witnessed) — the gap the s294/s295 handoffs flagged. ⚠️ **`check_ac_consistency` Check 3 was INERT for this PLAN** (it reads ONE line for an italicised `*Artifacts:*` clause; this PLAN puts `Artifact:` on the continuation line), so the ticks rest on §8's witness rule, not on the guard. 🔴 **CRAY'S, still open:** the object-synonym gap — (i) render object synonyms in the engine (own PR, before/after measured — Code's lean), (ii) a YAML workaround, (iii) leave it to AC-14 — and **AC-14's live smoke needs a typed §8 go**; it is the one unticked AC. Merged but never typed-ruled: golden row-4's registrar substitution, the three enums (`lowest_at_acceptance_basis` held only by its DB CHECK), the extra `quote_id` ref, title keys, reads with no tenant filter. ⚠️ **Deploying #1470 makes SD-D real on the public demo** — Cray's action. **Read:** `docs/plans/done/0109-*.md`.

> **Immediate next action is CODE — start the base-archive continuation file.** `docs/status-archive/2026-h1-status.md` closes this reconcile at about **193 KB**, roughly **3.5 KB** under R4's 192 KiB split trigger, so the next reconcile that rotates a row crosses it; R4 spills OLDER content into the next letter while the base keeps the recent window. Then **PLAN-0123 Step 4's AC-12 read** (first data: s294's two template goals, both instruments defective — the renderer was fixed s295), then Step 5 closeout. **CRAY:** PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go** — AC-14 is the single unticked AC on a now-closed PLAN; PLAN-0125 §8 (SD-1…SD-9, especially SD-5 and SD-6); the three classifier items (specimens five, s294); and the s294 fan-out's open question — **is a pilot live**, on which instance, since when? Then CODE commits the PLAN-0125 draft via a `docs/*` PR.

### Rotated at the s297 follow-up reconcile (#1478, #1479 merged) — the first s297 Recent-Decisions row, the PLAN-0123 Active-TODO row and the Next-Steps blockquote [all three REWRITTEN, not rotated: the s297 row now covers #1476–#1479 in place (one session, one row), the PLAN-0123 row moved from Step 4 owed to Step 5 waiting on Cray's AC-12 reading, and the blockquote's immediate action moved from Code's Step 4 read to Cray's reading of §11.2 — so this append is belt-and-braces under R4, as the s292–s294 reconciles did. No Current-Focus block and no ledger entry left the window (295–296, 297), so neither ledger changes and nothing goes to h1e. Each line probed ABSENT from every `-status` letter first, against a control derived from this file's own last line (=1). Carved from `git show HEAD:docs/STATUS.md`.]

| 2026-09-14 | **s297 (#1476) — R4 continuation split: sessions 226→273 left the base `-status` archive for the new `2026-h1i-status.md`; base 193,988 → 58,764 B.** ✅ Original body == `h1i` + one newline + base, byte for byte, against `git show 9803e22:`; battery 16/16 WITNESSED, GAPS 0; merge verified by blob. 🔴 `end-of-file-fixer` strips a carve's trailing blank line; `git checkout -- <path>` restores from the INDEX. | `554c9b4` / [#1476](https://github.com/CrayJThiemsert/vero-lite/pull/1476) / `docs/status-archive/2026-h1i-status.md` |

- [ ] **🆕 PLAN-0123 (goal declaration) — Steps 1–3 DONE; Step 3 ENDED AT 3.1 ON A MEASURED FAIL.** Step 1 #1459 (AC-1/2/3) · Step 2 #1461 + #1463 (AC-4…AC-8, 41/41 WITNESSED) · **Step 3.1 #1466 (s293): AC-9's replay read `p=27.1 %` against a 5 % ceiling — FAILED; AC-10 STRUCK (do not add `_reading_shape_advisory`); AC-11 unreachable (it waits on AC-10).** Remaining: Step 4 = AC-12 alone (s293 declared no template goal), then Step 5 closeout. **Read:** `docs/plans/0123-*.md` §11.1. 🔴 **AC-12's first data (s294), measured s295: both template goals had DEFECTIVE instruments.** `tools/goal_template.py:309` renders `--pattern` with `!r`, which doubles a backslash inside bash single quotes: the `\+` pattern read a vacuous `matched=0 PASS` on text that contains its target; the `\)` pattern read `not a valid regex`, rc=2, no evidence file — `fail` on all 5 evaluations. The PLAN-0109 claims themselves hold on `main` (intended patterns, positive controls). Archived s295 as `goal-history/20260911T125352-3cd3b8b4.json` (`disposition: cleared-unpassed`). ✅ **FIXED s295 (#1472, `4fb5174`):** every check parameter now renders through `shlex.quote` and T-ORACLE's C2 `re.escape`s the claim, witnessed 19/19 — a backslash in a template `--pattern` / `--claim` is safe on `main` at or after that commit. Older checkouts, including the stale worktrees, still mangle it.

> **Immediate next action is CODE — PLAN-0123 Step 4's AC-12 read** (the `goal-history/` counts plus Code's closeout narrative for Cray's reading; first data: s294's two template goals, both instruments defective — the renderer was fixed s295), then Step 5 closeout. **CRAY:** PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go** — the single unticked AC on a closed PLAN; PLAN-0125 §8 (SD-1…SD-9, especially SD-5 and SD-6); the three classifier items (specimens five, s294); and the s294 fan-out's open question — **is a pilot live**, on which instance, since when? Then CODE commits the PLAN-0125 draft via a `docs/*` PR.

### Rotated at the s298 reconcile — the oldest Recent-Decisions row (s286, #1429–#1434), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **679 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter with a 120-character needle against a control derived from this file's own last row (=1). The Recent-Decisions ledger's s295–296 entry left the window and is carried VERBATIM after the row rather than dropped on a token sweep.]

| 2026-09-08 | **s286 (#1429–#1434) — PLAN-0119 ratified `Accepted` (Cray, typed); Steps 1–2 complete, Step 3 part 1 landed: `Workload` is declared at construction with NO default, 8 sites migrated.** ✅ Taxonomy + call-site inventory shipped as a conventions doc; the intake recorder keeps load/prefill durations. 🔴 **#1432 corrected §3's inventory `was an error`** — 14 claimed vs 14 AST-measured, **not the same 14**. 🔴 AC-1/2/3/8/11 have evidence and NOT ONE is ticked. | `0bcc2ae` / [#1429](https://github.com/CrayJThiemsert/vero-lite/pull/1429) / [#1433](https://github.com/CrayJThiemsert/vero-lite/pull/1433) / `docs/conventions/llm-workload-taxonomy.md` |

_[The Recent-Decisions rotation ledger's s295–296 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s295–296) reconcile rotates the oldest row (s283 + s284, #1418–#1422)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Row is **805 B** as carved from `git show HEAD:`, probed ABSENT against a control **derived from the archive itself** rather than typed (=1) and a fabricated needle (=0). ⚠️ **The s293 entry left the window and was DROPPED** — the base archive's own `### Rotated at the s293 reconcile` header is that entry rewritten (`s281, #1408` = 1). 🔴 **The base archive closes this reconcile at ~193 KB, about 3.5 KB under R4's 192 KiB split trigger:** the next reconcile that rotates a row crosses it and owes the continuation file FIRST. ✅ **Discharged s297 (#1476):** base 193,988 → 58,764 B; s226–s273 spilled to `2026-h1i-status.md`.

### Rotated at the s298 reconcile — two completed Active-TODO rows and the Next-Steps blockquote [the runbook R4 row leaves because #1481 merged (`1a3ca99`); the PLAN-0123 row leaves because the PLAN is CLOSED 9 of 13 and archived (#1483), with AC-12 ruled B and SD-5 discharged by #1482 — its one remaining Cray item, the 34 orphan render dirs, continues in `blocked_on` and the Next-Steps blockquote. Both rows are HEAD's bytes with `[ ]` → `[x]` and one appended closing sentence. The blockquote was REWRITTEN — its immediate action, Cray's AC-12 reading, is done — so this copy is belt-and-braces under R4. Each probed ABSENT from every `-status` letter first. Carved from `git show HEAD:docs/STATUS.md`.]

- [x] **🆕 Runbook R4's `Live chains:` line is stale — it lists `2026-h1b/c/d/e/f/g-status.md`, missing `h1h` (s247) and now `h1i` (s297).** R4 itself says never to name a continuation from outside the archive, so the fix is a directory pointer, not another letter. Measured s297; a small `docs/*` PR. **Read:** `docs/runbooks/memory-architecture.md` §R4. ✅ **DONE s298 (#1481, `1a3ca99`):** the line now points at `ls docs/status-archive/`; both halves had rotted — the Current-Focus half also missed `d` and `e`.

- [x] **🆕 PLAN-0123 (goal declaration) — Steps 1–4 DONE; Step 5 WAITS ON CRAY'S AC-12 READING.** Step 1 #1459 · Step 2 #1461 + #1463 · Step 3 ended at 3.1 on a measured FAIL (#1466: AC-9 `p=27.1 %`, AC-10 struck, AC-11 unreachable — §11.1) · **Step 4 #1478 (s297): AC-12's numeric half NOT MET** — `template_goals=1 passed=0 real_findings=0`, unticked, `[judgment]` (§11.2). Its one template goal hit the `!r` defects #1472 fixed; older checkouts, the stale worktrees included, still mangle a backslash in a template pattern. The test leak §11.2 found is fixed (#1479). 🔴 **Cray:** read §11.2 and rule AC-12; Step 5 then settles whether SD-5's ADR-0018 edit is discharged or void (an Accepted-ADR edit is G1-gated). **Read:** `docs/plans/done/0123-*.md` §11.1–§11.2. ✅ **CLOSED s298 (#1483):** 9 of 13 ACs ticked on evidence re-read at `c39f4e3`; AC-12 ruled **B** (Cray, typed) and SD-5 = **X1**, discharged by the ADR-0018 amendment #1482. **Read:** `docs/plans/done/0123-*.md` §Closeout.

> **Immediate next action is CRAY — read PLAN-0123 §11.2 and rule AC-12** (numeric half NOT MET, `[judgment]`); Step 5's closeout and SD-5's ADR-0018 question wait on it. **Then CODE:** PLAN-0123 Step 5; any time, the runbook R4 `Live chains:` `docs/*` PR (Active TODOs). **CRAY, carried:** PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; PLAN-0125 §8 (SD-1…SD-9, especially SD-5 and SD-6); the classifier items (specimens five, plus two s297 candidates); whether to delete the 34 orphan render dirs left in `.claude/state/`; and the s294 fan-out's open question — **is a pilot live**, on which instance, since when? Then CODE commits the PLAN-0125 draft via a `docs/*` PR.

### Rotated at the s299 reconcile — the oldest Recent-Decisions row (s287, #1436–#1438), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **646 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter with a 120-character needle against a control derived from this file's own last row (=1). The Recent-Decisions ledger's s297 entry follows it verbatim.]

| 2026-09-09 | **s287 (#1436–#1438) — PLAN-0119 Steps 1–4 COMPLETE, 9 of 11 ACs ticked (Cray, typed); AC-9/AC-10 open by design.** 🔴 **The pre-tick re-run caught TWO batteries silently DEAD since s286** — a definition error, not a red probe; batteries do not run in CI and `check_ac_consistency` does not run them. ✅ Cray, typed: capacity provenance lives in the TYPE (`Term[T]`); SD-2's refusal gets its own client type. Six batteries: 102 claims, 92 RED, GAPS 0. | `ca0407d` / [#1436](https://github.com/CrayJThiemsert/vero-lite/pull/1436) / [#1438](https://github.com/CrayJThiemsert/vero-lite/pull/1438) / `docs/plans/0119-*.md` |

_[The Recent-Decisions rotation ledger's s297 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s297) reconcile rotates the oldest row (s285, #1424, #1425)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Row is **645 B** as carved from `git show HEAD:`, probed ABSENT from every `-status` letter (the base now holds s275 onward, `h1i` s226–s273) against a control derived from the base itself (=1). ⚠️ **The s293 and s294 entries left the window and were DROPPED:** their measured tokens read inside the base's own `### Rotated at the s293 reconcile` / `s294` headers, save s293's note that s288's entry sat outside the `(291, 292)` window, which `1794bc2`'s STATUS records. 🔴 `was an error` (s295–296): that reconcile declared the s293 entry DROPPED and left it here.

### Rotated at the s299 reconcile — two REWRITTEN Active-TODO rows and the Next-Steps blockquote [the PLAN-0125 row (two lines) moved from DRAFTED and uncommitted to COMMITTED with all nine SDs ruled (#1487); the freshness-guard row's threshold moved from Cray's open call to RULED SD-1 = (c), and its stale `drift=6` reading left; the blockquote's immediate action moved from Cray reading §8 to the go for Step 0. Carved from `git show HEAD:docs/STATUS.md`, each probed ABSENT here first against a control from the s298 section (=1).]

- [ ] **🆕 PLAN-0125 — measure-provenance pipeline (ADR-0038 C2's mechanism + OQ-5's guard) — DRAFTED s291 by the in-harness `plan-drafter`, 62,412 B, 16 ACs, 9 SDs, UNCOMMITTED until Cray reads §8.** ②+③+④ as ruled (Cray, typed, s291): a **tool-emitted** `measure/v1` block (value · procedure · `against_sha` · `declared_paths` · who/when · pre-fixed predicate, sealed by SHA-256), a four-mark fact-pack contract for `status-scribe` / `plan-drafter`, and `tools/check_measure_staleness.py`.
  🔴 **Two recommendations DEPART from the typed shape and are Cray's:** SD-5 (blocks live in `docs/logs/`, not STATUS frontmatter) and SD-6 (`explore-research` emits **no** block — it has no Bash either, so its block would be model-typed = prose). Behavioral claims (the s281 G20 shape) are named **out of scope** by the draft. Six items ELIMINATED rather than automated (§6).

- [ ] **🆕 STATUS freshness guard (s291, #1458) — `tools/check_status_freshness.py`, pre-commit `always_run + verbose`.** Exits 1 only on a broken `head_commit`; drift is PRINTED every commit, never gated (a zero-tolerance gate would redden every PR between reconciles). 🔴 **Threshold = PLAN-0125 SD-1**, Cray's. Read `STATUS-FRESHNESS: head=793b9d9 newest=58a46b7 drift=6 fresh=False` on merged `main` before this reconcile.

> **Immediate next action is CRAY — PLAN-0125 §8 (SD-1…SD-9, especially SD-5 and SD-6)**, then **CODE commits the PLAN-0125 draft via a `docs/*` PR**. PLAN-0123 is CLOSED (#1483) and ADR-0018 is amended (#1482). **CRAY, carried:** PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier items (specimens five, plus two s297 and three s298 candidates); whether to delete the 34 orphan render dirs left in `.claude/state/`; and the s294 fan-out's open question — **is a pilot live**, on which instance, since when?

### Rotated at the s301 reconcile — the oldest Recent-Decisions row (s288, #1440–#1444), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **670 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter against a control derived from this file's own last row (=1). The Recent-Decisions ledger's s298 entry follows it verbatim.]

| 2026-09-09 | **s288 (#1440–#1444) — the spine was TRUTH.** A published README described *discharged* gates in the present tense for three weeks; it and ADR-0032's Context were re-grounded, then guarded. 🔴 **2 of 21 probe batteries were silently DEAD** — `check_battery_definitions.py` now lints them at pre-commit. ✅ `run_query.py`'s silent aggregate drops REFUSED (a, typed). 🔴 **TEN instrument errors, ALL ad-hoc; no `goal.json` existed, so the Stop gate never fired.** | `8bdae71` / [#1440](https://github.com/CrayJThiemsert/vero-lite/pull/1440) / [#1444](https://github.com/CrayJThiemsert/vero-lite/pull/1444) / `tools/check_battery_definitions.py` |

_[The Recent-Decisions rotation ledger's s298 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s298) reconcile rotates the oldest row (s286, #1429–#1434)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Row is **679 B** as carved from `git show HEAD:`, probed ABSENT from every `-status` letter against a control derived from the base's own last row (=1). ⚠️ **The s295–296 entry left the window and was APPENDED VERBATIM** after the row in the base, not dropped on a token sweep.

### Rotated at the s301 reconcile — two REWRITTEN Active-TODO rows and the Next-Steps blockquote [the PLAN-0125 row moved from COMMITTED with Step 0 awaiting Cray's go to Step 0 SHIPPED (#1491) with Step 1 next; the freshness-guard row moved from a ruled-but-unbuilt threshold to a live gate; the blockquote's immediate action moved from Cray's go for Step 0 to Code's Step 1. The specimens row was only EXTENDED, so its unchanged original stays in STATUS and is not archived. Carved from `git show HEAD:docs/STATUS.md`, each probed ABSENT here first against a control from the s299 section (=1).]

- [ ] **🆕 PLAN-0125 — measure-provenance pipeline — COMMITTED `Draft` (#1487); all nine SDs RULED (Cray, typed, s299): `SD-1=c SD-2=c SD-3=a SD-4=a+_evidence SD-5=c SD-6=b SD-7=b SD-8=a SD-9=a`.** Next: **Step 0** on Cray's go — ④-lite gates a staged `docs/STATUS.md` on drift 0 (AC-17…AC-19). Ratification stays a separate act. Code accepted the reviser's S1–S5; Cray may overturn them. SD-9 = (a) deletes both ledgers' `Window =` at the first reconcile after ratification. **Read:** `docs/plans/0125-measure-provenance-pipeline.md` §8, §9.

- [ ] **🆕 STATUS freshness guard (s291, #1458) — `tools/check_status_freshness.py`, pre-commit `always_run + verbose`.** Exits 1 only on a broken `head_commit`; drift is PRINTED every commit. 🔴 **Threshold RULED — PLAN-0125 SD-1 = (c), Cray typed s299:** drift gates only when STATUS is staged; built as PLAN-0125 Step 0, not yet. The docstring's *"nine substantive"* was a total — #1486 corrected it to 9 commits, 3 substantive.

> **Immediate next action is CRAY — the go for PLAN-0125 Step 0** (SD-1 = c: ④-lite gates a staged `docs/STATUS.md` on drift 0; AC-17…AC-19), then **CODE executes Step 0 on a `chore/*` branch**. PLAN-0125 is COMMITTED (#1487) with all nine SDs ruled; ratification stays Cray's separate act. **CRAY, carried:** PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier items (specimens five, plus two s297 and five s298 candidates); whether to delete the 34 orphan render dirs left in `.claude/state/`; and the s294 fan-out's open question — **is a pilot live**, on which instance, since when?

### Rotated at the s302 reconcile — the oldest Recent-Decisions row (s289, #1445–#1448), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **689 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter against a control derived from the previous reconcile's own diff (=1). The Recent-Decisions ledger's s299 entry follows it verbatim.]

| 2026-09-09 | **s289 (#1445–#1448) — a slate session: every carried Cray call RULED, Step 4b rehomed out of `/tmp`, PLAN-0123 drafted.** ✅ #1446 tracked `extract_terms.py` + 11 planted-defect tests; the chain COMPLETED **9/9**. 🔴 Its headline is a TIMEOUT finding — **qwen ~19 tok/s vs gpt-oss ~46**, p95 124–126 s (max 142.4 s), above the shipped 120 s timeout. 🔴 A `check` pinned to `git show HEAD:` is valid only PRE-merge (now PLAN-0123 SD-2 = (c)). | `dad1b32` / [#1445](https://github.com/CrayJThiemsert/vero-lite/pull/1445) / [#1448](https://github.com/CrayJThiemsert/vero-lite/pull/1448) / `docs/plans/done/0123-*.md` · `benchmarks/intake_extraction/RESULTS.md` |

_[The Recent-Decisions rotation ledger's s299 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s299) reconcile rotates the oldest row (s287, #1436–#1438)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Row is **646 B** as carved from `git show HEAD:`, probed ABSENT from every `-status` letter against a control derived from the base's own last row (=1). ⚠️ **The s297 entry left the window and was APPENDED VERBATIM** after the row in the base. Also to the base: the REWRITTEN PLAN-0125 and freshness-guard Active-TODO rows and the Next-Steps blockquote.

### Rotated at the s302 reconcile — the REWRITTEN PLAN-0125 Active-TODO row and the Next-Steps blockquote [the PLAN-0125 row moved from Step 0 SHIPPED with Step 1 next to Steps 0–1 SHIPPED (#1493) with Step 2 next; the blockquote's immediate action moved from Code's Step 1 to Code's Step 2. The specimens and tools-catalogue rows were only EXTENDED, so their unchanged originals stay in STATUS and are not archived. Carved from `git show HEAD:docs/STATUS.md`, each probed ABSENT here first against a control from the s301 section (=1).]

- [ ] **🆕 PLAN-0125 — measure-provenance pipeline — Step 0 SHIPPED (#1491, `75cd580`): the reconcile gate is live, battery 27/27 WITNESSED.** AC-17…AC-19 stay unticked until closeout. Next: **Step 1** — `tools/measure.py` + `tests/tools/test_measure.py` + the `docs/logs/` fixture (AC-1…AC-5, AC-20 half), branch `chore/s3NN-plan0125-step1-measure-emitter`. Ratification is Cray's separate act; SD-9 = (a) deletes both ledgers' `Window =` at the first reconcile after it. S1–S5 accepted by Code; Cray may overturn. **Read:** `docs/plans/0125-measure-provenance-pipeline.md` §Steps, §8, §9.

> **Immediate next action is CODE — PLAN-0125 Step 1** (`tools/measure.py`, AC-1…AC-5) on a `chore/*` branch. ⚠️ Since #1491 a reconcile must start from a fresh `main` and name main's last merge commit as `head_commit`, or its own commit reds. **CRAY, carried:** PLAN-0125 ratification; PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier items (specimens five, plus nine candidates); whether to delete the 34 orphan render dirs in `.claude/state/`; whether to prune the 9 prunable worktrees (all with UNC gitdirs; s299 recorded 2); and **is a pilot live**, on which instance, since when?

### Rotated at the s303 reconcile — the oldest Recent-Decisions row (s290, #1449–#1452), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **690 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter first. The Recent-Decisions ledger's s301 and s302 entries follow it verbatim.]

| 2026-09-10 | **s290 (#1449–#1452) — Stop-classifier transport REPAIRED, AC-12's blocker NAMED.** 🔴 `transport` was a LOSSY label: over 187 records split by `reason`, **136 (72.7%)** answered fast and wrong, only **2 (1.1%)** were network — root cause is unmappable `gpt-oss:20b` harmony tool calls (90 Ollama warnings, 45/45 across two categories). ✅ Retry ×3 + `http_error` ≠ `timeout`, 9 probes WITNESSED. 🔴 The proceed-arm reason-quality defect fired LIVE, UNFIXED. | `793b9d9` / [#1449](https://github.com/CrayJThiemsert/vero-lite/pull/1449) / [#1452](https://github.com/CrayJThiemsert/vero-lite/pull/1452) / `docs/plans/0122-*.md` AC-12 · `docs/lessons/0062-*.md` |

_[The Recent-Decisions rotation ledger's s301 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s301) reconcile rotates the oldest row (s288, #1440–#1444)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Row is **670 B** as carved from `git show HEAD:`, probed ABSENT from every `-status` letter against a control derived from the base's own last row (=1). ⚠️ **The s298 entry left the window and was APPENDED VERBATIM** after the row in the base. Also to the base: the REWRITTEN PLAN-0125 and freshness-guard Active-TODO rows and the Next-Steps blockquote (the specimens row was only extended, so its original stays here).

_[The Recent-Decisions rotation ledger's s302 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s302) reconcile rotates the oldest row (s289, #1445–#1448)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten. Row is **689 B** as carved from `git show HEAD:`, probed ABSENT from every `-status` letter against a control derived from the previous reconcile's own diff (=1). ⚠️ **The s299 entry left the window and was APPENDED VERBATIM** after the row in the base. Also to the base: the REWRITTEN PLAN-0125 Active-TODO row and the Next-Steps blockquote (the specimens and tools-catalogue rows are only extended, so their originals stay here).

### Rotated at the s303 reconcile — the REWRITTEN PLAN-0125 Active-TODO row and the Next-Steps blockquote [the PLAN-0125 row moved from Steps 0–1 SHIPPED with Step 2 next to Steps 0–2 SHIPPED (#1496) with Step 3 / Step 5 next; the blockquote's immediate action moved from Code's Step 2 to Code's Step 3 or the Step 5 closeout. The specimens row was only EXTENDED (the s303 record at log line 443), so its unchanged original stays in STATUS and is not archived. Carved from `git show HEAD:docs/STATUS.md`, each probed ABSENT here first.]

- [ ] **🆕 PLAN-0125 — measure-provenance pipeline — Steps 0–1 SHIPPED (#1491 `75cd580`, #1493 `df5beb6`): the reconcile gate is live; the emitter and its first six blocks are in.** No AC ticked (AC-5 closes on Step 2's guard line; other ticks Cray's). Next: **Step 2** — `tools/check_measure_staleness.py` + test + pre-commit (AC-6…AC-11, AC-20 half). 🔴 Cray's, before AC-11: detect-secrets flags every full-hex `against_sha`. Ratification is Cray's; SD-9 = (a) deletes both ledgers' `Window =` at the first reconcile after it. **Read:** `docs/plans/0125-measure-provenance-pipeline.md` §3, §Steps.

> **Immediate next action is CODE — PLAN-0125 Step 2** (`tools/check_measure_staleness.py`, AC-6…AC-11) on a `chore/*` branch. ⚠️ Reconcile only from a fresh `main`, naming main's last merge commit as `head_commit`, or its own commit reds. **CRAY, carried:** PLAN-0125 ratification and the AC ticks; detect-secrets vs the full-hex `against_sha`, before Step 2's AC-11; PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier items (specimens five, plus eleven candidates); whether to delete the 34 orphan render dirs in `.claude/state/`; whether to prune the 7 registered prunable worktrees; and **is a pilot live**, on which instance, since when?

### Rotated at the s307 reconcile — the oldest Recent-Decisions row (s291, #1457–#1459), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **944 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter first. The Recent-Decisions ledger's s303 entry follows it verbatim.]

| 2026-09-10 | **s291 (#1457–#1459) — a verification survey found NO shipped guard re-derives a fact; PLAN-0125 drafted to own the gap.** 🔴 Every guard checks reference integrity or pairwise agreement — `check_ac_consistency.py` says so in its own docstring; the cause is structural (`status-scribe`/`plan-drafter` carry `Bash` in `disallowedTools`), and ADR-0038 OQ-5's ownerless mechanism is now **PLAN-0125** (16 ACs, 9 SDs, UNTRACKED). ✅ #1457 tools catalogue (20 entries); #1458 the ④-lite freshness guard (drift printed, never gated); **#1459 PLAN-0123 Step 1 — 8/8 WITNESSED, ×3 batteries**. ⚠️ The live `/goal` first read `C1=C2=C3=fail` on green tests — the Stop hook runs Windows-side. | `b179825` / [#1457](https://github.com/CrayJThiemsert/vero-lite/pull/1457) / [#1459](https://github.com/CrayJThiemsert/vero-lite/pull/1459) / `docs/plans/0125-*.md` · `tools/README.md` · `tools/check_status_freshness.py` |

_[The Recent-Decisions rotation ledger's s303 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s303) reconcile rotates the oldest row (s290, #1449–#1452)** to `2026-h1-status.md` on the **count rule alone** — one row entered, so one left to hold the table at ten; the row is emitted VERBATIM in the scribe's return for Code to append, carved from the pre-edit file rather than `git show HEAD:` (this seat has no shell). ⚠️ **The s301 and s302 entries left this ledger with the Current-Focus window they are declared level with**, and are emitted verbatim too. Also to the base: the REWRITTEN PLAN-0125 Active-TODO row and the Next-Steps blockquote (the specimens row was only extended and the PLAN-0126 row is new, so no other original leaves here).

### Rotated at the s307 reconcile — the REPLACED PLAN-0125 Active-TODO row, the REMOVED PLAN-0126 Active-TODO row and the Next-Steps blockquote [the PLAN-0125 row moved from Steps 0–2 SHIPPED with Step 3 / Step 5 next to Steps 0–3 SHIPPED (#1507, #1508) with Step 4 / Step 5 next; the PLAN-0126 row was open only for the archive move, which #1502 made (PLAN-0126 verified in `docs/plans/done/` on main); the blockquote's immediate action moved from Code's Step 3 to Code's Step 4 or the Step 5 closeout. The specimens row was only EXTENDED (the s307 records at log lines 514–515), so its unchanged original stays in STATUS and is not archived. Carved from `git show HEAD:docs/STATUS.md`, each probed ABSENT from every `-status` letter first.]

- [ ] **🆕 PLAN-0125 — measure-provenance pipeline — Steps 0–2 SHIPPED (#1491 `75cd580`, #1493 `df5beb6`, #1496 `8b13e18`): the reconcile gate, the emitter and the staleness guard are all live.** No AC ticked — the ticks wait on the Step 5 closeout and Cray's ratification. Next: **Step 3** (the fact-pack contract, AC-12…AC-15) or **Step 5 closeout**, which needs 🔴 Cray's detect-secrets vs full-hex `against_sha` call first. SD-9 = (a) deletes both ledgers' `Window =` at the first reconcile **after** ratification — not ratified yet, so both stay. **Read:** `docs/plans/0125-*.md` §3, §Steps (s303's D1/D2 addendum is in §3.2).

- [ ] **🆕 PLAN-0126 (fleet story-mode explainer at `/story/`) — CLOSED OUT: AC-1 … AC-11 all ticked (`ticked=11 unticked=0` on main), #1495–#1501.** Deployed to the fleet system under Cray's typed per-phase go — `pre=404 post=200 csp=present`, `DEMO-STATE: PRISTINE`. AC-3/AC-7 shipped as **option (C)**: artifact lists re-pointed **and** the two missing witnesses added. 🔴 **Open only for the archive move** — #1502 moves the PLAN to `docs/plans/done/` once it is updated onto main, so cite **PLAN-0126 by number, never by path** (R8). **Read:** PLAN-0126 §Closeout.

> **Immediate next action is CODE — PLAN-0125 Step 3** (the fact-pack contract, AC-12…AC-15) **or the Step 5 closeout**, on a branch; the closeout waits on Cray's detect-secrets vs full-hex `against_sha` call. ⚠️ Reconcile only from a fresh `main`, naming main's last merge commit as `head_commit`, or its own commit reds. **CRAY, carried:** PLAN-0125 ratification and the AC ticks; detect-secrets vs the full-hex `against_sha`, before the Step 5 closeout block; PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier items (specimens five, plus the `proceed`→`block` candidates through log line 443); whether to delete the 34 orphan render dirs in `.claude/state/`; whether to prune the 7 registered prunable worktrees; and **is a pilot live**, on which instance, since when? **Discharged this window:** PLAN-0126 — #1502 only moves it to `docs/plans/done/`.

### Rotated at the s308 reconcile — the oldest Recent-Decisions row (s293, #1466), on the count rule alone [one row entered, so one left to hold the table at ten. Row is **503 B** as carved from `git show HEAD:docs/STATUS.md`, probed ABSENT from every `-status` letter first. The Recent-Decisions ledger's s307 entry follows it verbatim.]

| 2026-09-11 | **s293 (#1466) — PLAN-0123 Step 3.1: AC-9's kill criterion FAILED; AC-10 struck, AC-11 unreachable.** 🔴 The replay read `p=27.1 %` (162 of 597 Bash calls) against a 5 % ceiling; `grep -c` or `wc -l` is in 114 of the 162. The fires were well aimed (9 of 18 valid), but clause 3's `X = V = 9` is NOT robust. The hook was never opened. | `da0c900` / [#1466](https://github.com/CrayJThiemsert/vero-lite/pull/1466) / `docs/plans/done/0123-*.md` §11.1 · `tools/reading_shape_replay.py` |

_[The Recent-Decisions rotation ledger's s307 entry, verbatim from `git show HEAD:docs/STATUS.md`:]_ 🔴 **THIS (s307) reconcile rotates the oldest row (s291, #1457–#1459)** to `2026-h1-status.md` on the **count rule alone** — one row (s304–s307) entered, so one left to hold the table at ten; the row is emitted VERBATIM in the scribe's return for Code to append, carved from the pre-edit file rather than `git show HEAD:` (this seat has no shell). ⚠️ **The s303 entry left this ledger with the Current-Focus window it was declared level with**, and is emitted verbatim too. Also to the base, from dispatch 2: the removed PLAN-0126 Active-TODO row and the replaced originals of the PLAN-0125 Active-TODO row and the Next-Steps blockquote (the specimens row was only extended, so no original of it leaves).

### Rotated at the s308 reconcile — the RESOLVED s306 In-Flight entry and the Next-Steps blockquote [the In-Flight entry announced its own resolution — Cray's s306 advisory ruling, now carried by the s306 + s308 Recent-Decisions row; the blockquote's immediate action moved from Code's PLAN-0125 Step 4 / Step 5 closeout to Cray's #1512 merge and #1513 ratification. The specimens row was only EXTENDED (log lines 530, 543 and 551), so its unchanged original stays in STATUS and is not archived. Carved from `git show HEAD:docs/STATUS.md`, each probed ABSENT from every `-status` letter first.]

- **RESOLVED s306 — a skill's registry table does NOT bind.** Cray ruled (typed, s306): the four-stream registry in `.claude/skills/stream-status/SKILL.md` is **advisory** ("โน้ตช่วยจำ"), chosen over promoting it to binding. Code's implementation (#1510, the cleanup Cray approved): pointers only, no same-PR update obligation, a reader-side check in that skill's step 2; `tools/check_plan_archive_refs.py` (R8) still reddens the archive of any PLAN the table names. Opened s210; move to Recent Decisions at the next reconcile.

> **Immediate next action is CODE — PLAN-0125 Step 4** (the SD-slot premise stamp in `plan-drafter.md`, SD-3 = a) **or the Step 5 closeout**, on a branch; the closeout waits on Cray's detect-secrets vs full-hex `against_sha` call. ⚠️ Reconcile only from a fresh `main`, naming main's last merge commit as `head_commit`, or its own commit reds. **CRAY, carried:** **PLAN-0125 AC-14's typed read** of §3.1 + §5 (asked in #1507), then ratification and the AC ticks; detect-secrets vs the full-hex `against_sha`, before the Step 5 closeout block; **s306's corrective PR for the four defects #1506 left on main**; PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier items (specimens five, plus the `proceed`→`block` candidates through log line 515); whether to delete the 34 orphan render dirs in `.claude/state/`; whether to prune the 7 registered prunable worktrees; and **is a pilot live**, on which instance, since when? **Discharged this window:** PLAN-0125 Step 3 (#1507, #1508); PLAN-0126's archive move (#1502).


### Rotated at the s309 reconcile — the s294 Recent-Decisions row and the units it replaced

| 2026-09-11 | **s294 (#1468–#1470) — PLAN-0109 Phases 1–2 shipped (Ask over repair cases); 0 of 14 ACs ticked, closeout owed.** ✅ Four ruled-content defects fixed first; the ontology↔ORM lockstep guard reads `offenders=0`; batteries 35/35 + 38/38 WITNESSED, GAPS 0. 🔴 Object synonyms never reach the translate prompt — Cray's (i)/(ii)/(iii). 🔴 s295: both s294 template goals had defective instruments (`goal_template.py` `!r` quoting). | `a0bdc17` / [#1468](https://github.com/CrayJThiemsert/vero-lite/pull/1468) / [#1470](https://github.com/CrayJThiemsert/vero-lite/pull/1470) / `docs/plans/done/0109-*.md` · `tools/check_ontology_orm_lockstep.py` |

_[The Recent-Decisions rotation ledger's s308 entry, verbatim:]_ 🔴 **THIS (s308) reconcile rotates the oldest row (s293, #1466)** to `2026-h1-status.md` on the **count rule alone** — one row (s306 + s308) entered, so one left to hold the table at ten; the row is emitted VERBATIM in the scribe's return, and Code carves it from `git show HEAD:docs/STATUS.md` to append. ⚠️ **The s307 entry left this ledger with the Current-Focus window it was declared level with**, and is emitted verbatim too. Also to the base: the RESOLVED s306 In-Flight entry (its ruling is now in the new row) and the replaced Next-Steps blockquote (the specimens row was only extended, so its original stays).


_[The Story-page In-Flight entry, replaced at s309 (the v2a fixes and the act-0 disclosure shipped in #1517 / #1518):]_

- **Story page (PLAN-0126 v1) — the reviewed improvement plan waits on Cray (s306).** Unanswered: whether to start the v2a fixes (the Act 5 label overlap — agreed, redeploy per-phase go; a sub-line naming the wrong step's `llm_assist`); content decisions D-A…D-E; G1, a sole-source fourth case amending L5; G2–G4, a primer scene; G7, rehoming R9. 🔴 The plan exists only in the gitignored s306 CLOSE handoff (Appendix A) — no tracked home.

_[The PLAN-0127 Active-TODO row, replaced at s309 (ratified in #1515):]_

- [ ] **🆕 PLAN-0127 — codegen generation stability (A): `Draft` in #1513 (not on main); its PR-1 is #1512 (`fix/*`, CI PASS).** Cray: merge #1512, ratify #1513 and rule SD-1…SD-7 — SD-2 is #1512's ordering mechanism, so a countermand costs least before that merge. PR-2…PR-5 wait for ratification; **(B), runtime adoption, is not opened** (Cray, typed s306). PR-5 would discharge the Counterparty row's `generate core` and stale-runbook items. **Read:** #1513.

_[The generator Active-TODO row, discharged at s309 by #1516 and deleted from STATUS:]_

- [ ] **🆕 The PLAN-0126 story batteries' generator has no tracked home.** Its only copy is `.claude/handoffs/session-304/gen_batteries_plan0126.py` (gitignored), and any story change that bumps `story.js?v=` must regenerate the page, drift and scenario batteries with it (s306). Give it a tracked home before the next regeneration.

_[The Next-Steps blockquote, replaced at s309:]_

> **Immediate next action is CRAY — merge #1512** (PLAN-0127 PR-1, `fix/*`, CI PASS) **and ratify #1513** (PLAN-0127 `Draft`, SD-1…SD-7); Code then runs PLAN-0127 PR-2…PR-5. **Code, otherwise:** PLAN-0125 Step 4 (the SD-slot premise stamp) or the Step 5 closeout, which waits on Cray's detect-secrets vs full-hex `against_sha` call. ⚠️ Reconcile only from a fresh `main`, naming main's last merge commit as `head_commit`, or its own commit reds. **CRAY, carried:** the story-page decisions (s306, In-Flight) and a time for the live-structuring discussion; **PLAN-0125 AC-14's typed read** of §3.1 + §5, then ratification and the AC ticks; detect-secrets vs the full-hex `against_sha`; PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier items (specimens five, plus the `proceed`→`block` candidates through log line 551); whether to delete the 34 orphan render dirs in `.claude/state/`; whether to prune the registered prunable worktrees; and **is a pilot live**, on which instance, since when? **Discharged this window:** #1506's corrective PR (#1510); the stale codegen docstrings (#1511).


### Rotated at the s310 reconcile — the oldest Recent-Decisions row, its ledger entry, the DISCHARGED story `?v=` TODO and the replaced Next-Steps blockquote

_[The oldest Recent-Decisions row (s295–s296, 933 B), rotated on the count rule alone — the s310 row entered, so one left to hold the table at ten:]_

| 2026-09-12 | **s295–s296 (#1472–#1474) — PLAN-0109 CLOSED 13 of 14 ACs on re-measured evidence; the s295 `.git/config` incident is now structurally guarded.** ✅ `tests/conftest.py` strips every inherited `GIT_*` at import (ALLOW-list; `GITHUB_*` survives, witnessed) — battery **8/8, GAPS 0**. ✅ AC-5's golden oracle finally carries a battery (2 witnessed; 36 of 38 claims exempted as the scaffolder's own, stated rather than hidden behind COMPLETE). 🔴 **Two premises fell:** `check_ac_consistency` Check 3 is **INERT** for PLAN-0109 (artifact on a continuation line), and the s295 writer list named a module whose helpers have no call sites. **AC-14 stays unticked — host-state, no typed go.** | `638867d` / [#1473](https://github.com/CrayJThiemsert/vero-lite/pull/1473) / [#1474](https://github.com/CrayJThiemsert/vero-lite/pull/1474) / `docs/plans/done/0109-*.md` · `tests/batteries/s296-git-env-guard.json` |

_[The Recent-Decisions rotation ledger's s309 entry, verbatim:]_ _[Recent-Decisions rotation ledger — **CURRENT window only** (R2; the ledger's own window plus a ~900 B per-entry cap, Cray s267); earlier entries travel with their rows into [`2026-h1-status.md`](status-archive/2026-h1-status.md). Window = **309** — ONE, level with the Current-Focus window (**309**). 🔴 **THIS (s309) reconcile rotates the oldest row (s294, #1468–#1470; 671 B)** to `2026-h1-status.md` on the **count rule alone** — one row (s309) entered, so one left to hold the table at ten; the row is emitted VERBATIM in the scribe's return, and Code carves it from `git show HEAD:docs/STATUS.md` to append. ⚠️ **The s308 entry left this ledger with the Current-Focus window it was declared level with**, and is emitted verbatim too. Also to the base: the replaced Story-page In-Flight entry, the deleted generator TODO row (335 B, discharged by #1516), the rewritten PLAN-0127 TODO row and the replaced Next-Steps blockquote.]_

_[The story `?v=` Active-TODO row, DISCHARGED at s310 by #1521 and deleted from STATUS. The gap it names is closed: the cache-bust check now discovers every static `index.html` and locks the set.]_

- [ ] **🆕 The story `?v=` tokens are unguarded in CI.** `tools/ci/cache_bust_diff_check.py` covers `static/assets/` and the console `index.html` only, so a changed `static/story/` file behind an unchanged token ships silently (it read `0 bumped` across #1517 and #1518). A task is running in another session (spawned s309); reconcile its result when it lands.

_[The Next-Steps blockquote, replaced at s310:]_

> **Immediate next action is CRAY — ratify PLAN-0128** (#1519, `Draft` on main; **SD-a first**). **Code, meanwhile:** PLAN-0127 PR-2 (`feat/codegen-every-doc-suite`) and Step 1b (measure blocks) — both unblocked and independent; else PLAN-0125 Step 4 (the SD-slot premise stamp) or the Step 5 closeout, which waits on Cray's detect-secrets vs full-hex `against_sha` call. ⚠️ Reconcile only from a fresh `main`, naming main's last merge commit as `head_commit`, or its own commit reds. **CRAY, carried:** the story deploy's per-phase go and who runs it (Cray — the classifier refuses Code's `ssh ms-s1`); the remaining story content decisions (D-A…D-E, G1, G2–G4, G7); a time for the live-structuring discussion; **PLAN-0125 AC-14's typed read** of §3.1 + §5, then ratification and the AC ticks; detect-secrets vs the full-hex `against_sha`; PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier specimens (now through log line **564**; 566 lines); whether to delete the 34 orphan render dirs in `.claude/state/`; whether to prune the registered prunable worktrees; and **is a pilot live**, on which instance, since when? **Discharged this window:** the generator's missing home (#1516); the story v2a fixes and the act-0 disclosure (#1517, #1518); PLAN-0127's ratification (#1515).


### Rotated at the s311 reconcile — 2026-09-18

_[RD row (s297), verbatim:]_

| 2026-09-14 | **s297 (#1476–#1479) — the base `-status` archive split to `h1i` (193,988 → 58,764 B, byte for byte); PLAN-0123 Step 4 read AC-12 NOT MET (`template_goals=1 passed=0 real_findings=0`), unticked for Cray; the renderer's tests stopped writing the checkout's `.claude/state/`.** 🔴 The lifecycle test leaked 4 dirs a run (36 → 40); now `CLAUDE_GOAL_STATE_DIR` + a conftest import-time guard, and a full suite holds 36 → 36. | `1949329` / [#1478](https://github.com/CrayJThiemsert/vero-lite/pull/1478) / [#1479](https://github.com/CrayJThiemsert/vero-lite/pull/1479) / `docs/plans/done/0123-*.md` §11.2 |

_[replaced TODO row — PLAN-0128 ratification, verbatim:]_

- [ ] **🆕 PLAN-0128 — probe-battery claim tags, `Draft` on main (#1519).** Cray ratifies and rules SD-a…SD-f — **SD-a first** (does the tag derivation reopen PLAN-0115 AC-5, or keep it?): a ruling for a load-time reference instead means the PLAN is re-drafted, not amended. Step 3 retires #1516's generator, its guard and the guard's battery together. **Read:** `docs/plans/0128-*.md`.

_[replaced TODO row — handoff gate, verbatim:]_

- [ ] **🔴 🆕 LIVE NOW — the `handoff-frontmatter` pre-commit gate is SILENCED; s310 caused it.** An empty `.claude/handoffs/session-310/`, created to claim the session number, was enough: `precommit_handoffs.py` scopes to the **highest-numbered** session dir only ("no legacy drag", s35), so an empty newest dir yields zero files and the hook **exits 0 having checked nothing**. Re-measured independently at s310 via `main([root])` in-process — **A** malformed in the newest dir `rc=1`; **B** malformed in an older dir, newest empty **`rc=0`**; **C** same without the empty dir `rc=1`. **Blast radius LOW** (handoffs are gitignored — nothing reaches a commit) and it **self-heals** once any handoff lands in a higher dir. Durable defect: the hook cannot tell *"nothing to check"* from *"checked and clean"* — a vacuous green (§8), true independently of the dir. Fixes, all Cray's: (a) put a real handoff there — a worktree session **cannot**, the harness blocks writes to the shared `.claude/`; (b) delete the dir, surrendering the number claim; (c) make the two outcomes distinguishable — **the only one that outlives this**. s310 took none: (b) and (c) touch state other live sessions depend on. ⚠️ `main()` takes an `argv` root but `__main__` calls it with none, so a CLI run hits the REAL repo, `write_index()` included.

_[replaced Next-Steps blockquote, verbatim:]_

> **Immediate next action is CRAY — ratify PLAN-0128** (#1519, `Draft` on main; **SD-a first**). **Code, meanwhile:** PLAN-0127 PR-2 (`feat/codegen-every-doc-suite`) and Step 1b (measure blocks) — both unblocked and independent; else PLAN-0125 Step 4 (the SD-slot premise stamp) or the Step 5 closeout, which waits on Cray's detect-secrets vs full-hex `against_sha` call. ⚠️ Reconcile only from a fresh `main`, naming main's last merge commit as `head_commit`, or its own commit reds. **CRAY, carried:** the story deploy's per-phase go and who runs it (Cray — the classifier refuses Code's `ssh ms-s1`); the remaining story content decisions (D-A…D-E, G1, G2–G4, G7); a time for the live-structuring discussion; **PLAN-0125 AC-14's typed read** of §3.1 + §5, then ratification and the AC ticks; detect-secrets vs the full-hex `against_sha`; PLAN-0109's object-synonym gap (i)/(ii)/(iii) and **AC-14's typed §8 go**; the classifier specimens (measured at s309: through log line **564**; 566 lines); whether to delete the 34 orphan render dirs in `.claude/state/`; whether to prune the registered prunable worktrees; **the Active-TODOs rotation call** (new this window); and **is a pilot live**, on which instance, since when? **Discharged this window:** the story `?v=` CI gap (#1521) — the only TODO s310 closed. **Opened:** the `status-scribe`-in-a-worktree hook gap and the Active-TODOs rotation call. _[s309's discharges (#1515–#1518) left with its Current-Focus block.]_
