# PLAN-0042: The AT-2 / managerial-layer BUILD (ADR-0025) — type the AT-2 governance content, make the run-gate AT-2-aware (close the live blindness defect), migrate the shipped procurement AT-2 behind a green golden test

**Status:** Ready for execution — OQ-A=A1, OQ-B=B2 **ratified** (Cray, session 85); OQ-C/D/E confirmed
**Owner:** Claude Code
**Created:** 2026-06-29
**Revised:** 2026-06-29 (session 85 — recorded the OQ-A=A1 / OQ-B=B2 ratification; folded the Code-R2 SoD principal-vs-role scoping finding under A1; marked AC-13-ALT [the A2 run path] deferred to a follow-on PLAN)
**Related ADRs:** ADR-0025 (the decision spine — D1–D8 + the Implementation-Notes 6-step order + the OQ-1..5 ratification stamp; this PLAN *renders* its Implementation Notes and owns the migration sequencing OQ-5 assigned it), ADR-0024 (D3 the mechanical "governed ≠ generated" + the G/H/D partition this extends; D7 AT-2 deferral revisited by ADR-0025; OQ-8 typed-content home decided by ADR-0025; PLAN-0040 is the prior build of this layer), ADR-016 (D2 the `Step`/`Procedure`/`Agent` grammar; **D2-A4** facet non-authoritative at run time; D2-A3 the `in_file_band → Step.threshold` "point-at, don't re-store" precedent this mirrors; the run path OQ-A extends, **not a new engine**), ADR-007 (the `RecommendedAction` approve→execute write gate — **untouched**; the only path to an irreversible write), ADR-0019 / ADR-010 IN-3 (the determinism invariant — route on the engine verdict, never the LLM signal), ADR-006 D4 (Rule of Three — the AT-2 generator stays deferred at N=1; the D7 N≥2 re-trigger), ADR-009 D1/D2 (Cowork drafts ungated, only Code commits), ADR-012 D4.3 (author≠reviewer disclosure), ADR-013 (Cowork = advisory governance drafter). Builds directly on PLAN-0040 (the draft layer + `validate_governance_complete` + the prose-lint this extends) and PLAN-0039 (the read-only facet viewer the D4 provenance banding reuses). Substrate: `services/engine/procedures/{spec.py,draft.py,orchestrator.py,prose_lint.py}`, `verticals/procurement/procedures.yaml`, `docs/conventions/procedure-archetypes.md`.

> **Provenance / author≠reviewer disclosure (ADR-012 D4.3).** Originator = Cray (the s84 locked O-1→O-3→O-2→O-4 sequence + the explicit s85 typed instruction "เริ่ม O-3 follow-on build PLAN" — a real Cray decision to *build* O-3) + ADR-0025 OQ-5 (which named this follow-on PLAN as the build vehicle and assigned it the migration sequencing) + Code (the s85 dispatch that scoped the build, re-verified the live defect on HEAD, and surfaced two substrate nuances — OQ-B the value-authoring gap, OQ-D the prose-lint scope clash). Drafter = Cowork (Tier-1 governance authoring, ADR-009 D1 interim per ADR-013 phased relocation). Reviewers = Cray (**ratified session 85: OQ-A=A1, OQ-B=B2, OQ-C/D/E confirmed** — see §Surfaced Open Questions) + Code R2 (the Delta-2 SoD principal-vs-role scoping finding, then commit). The substance descends from the Accepted ADR-0025, **not** from Cowork's own free-form deliberation, so the independent-deliberation check is exercised; the §"Surfaced Open Questions" forks (OQ-A…E) were presented with options + a recommendation and are now **Cray-ratified** (session 85), **not** silently resolved; the Delta-2 SoD principal-vs-role correctness finding originated with **Code's independent R2 review** (ADR-012 D4.3 author≠reviewer). **Build PLAN — NO new ADR** (ADR-0025, Accepted #463, already decided the construct). Drafted **uncommitted**; this revision applies three surgical deltas after Cray ratified (session 85). Code R2-reviews **the delta** + commits via a `docs/*` PR (ADR-009 D2). **Cowork does not git** (G5 binds).

---

## Goal

Build the AT-2 / managerial-process layer **exactly as ADR-0025 D1–D8 decided** and **close the live run-gate AT-2-blindness defect (D5)** — in a correctly-sequenced set of PRs whose **offline oracle (D8) is the merge gate** (no live MS-S1; generation is out of scope). Concretely: (1) give AT-2's governance **content** a typed, human-only, authoritative home (D2: the discriminated `Step.governance_content` union + `Procedure.separation_of_duties`) with the D3 validators that make bypass **unrepresentable**; (2) extend `derive_governance_todo` / `validate_governance_complete` to **semantic** AT-2 completeness (D5) so a hollow-but-complete AT-2 skeleton is **refused**; (3) **migrate the shipped procurement AT-2 (`emergency_sourcing_round`) from prose to typed content in the SAME PR as the D5 gate extension, behind a green golden test** (the migration trap — the shipped procedure fails `validate_governance_complete` the moment the obligations land); (4) hold **governed ≠ generated** at the AT-2 authoring surface (D4: the new fields are H / human-author-only; recursive disjointness + `StepFacet`-unreachability CI checks; a scoped prose-lint + provenance banding); (5) per OQ-A, either run the AT-2 procedure under the four D6 guarantees or ship **author + render only** — the concrete v1 call is **this PLAN's**, per the four-guarantee feasibility. The thesis preserved: this builds the **layer**, not the **generator** (D7 — deferred, N=1; the classify abstain path stays — PLAN-0041 LOCKED). It extends the shipped engine (ADR-016) and the shipped draft layer (ADR-0024) — **not** a new engine.

## Fact-pack findings (verified against the live repo — Tier-1 rule #4)

Verified on the working tree (Read/Glob/Grep over the UNC mount; the dispatch's HEAD `1305b32` is one commit ahead of STATUS's `head_commit: f56a6e8` / `recent_commits[0]: 17789ef` — see the env note in the closeout; the substrate below is confirmed against the live files, which is what binds the build). These confirm the §4 substrate Code scouted and surface **one divergence (finding 1) the dispatch did not name** — flagged rather than silently absorbed.

1. **DIVERGENCE — a `tiers` naming collision the build MUST route around.** `Step.tiers` **already exists** (`spec.py:264`, type `StepTiers` = the canonical/acceptable/forbidden *handler-tier taxonomy* from PLAN-0022, SD-5=a) and is **already a member of `STEP_GOVERNANCE_FIELDS`** (`draft.py:43`). D2's DOA ladder also has a `tiers` (`DoaLadder.tiers: list[DoaTier]`). These are **different concepts**. The build MUST NOT add a second top-level `Step.tiers` for DOA — the DOA `tiers` live **nested inside** `governance_content` (`DoaLadder.tiers`), never as a sibling of the existing `Step.tiers`. (Surfaced so the PLAN does not silently shadow a shipped field.)
2. **The live defect is exactly as Code R2 described (the ADR prose simplified it — the PLAN must be precise).** `derive_governance_todo` (`draft.py:201–217`): an `EVALUATE` step with `gate_kind` other than `in_file_band`/`env_band` → `[]`; an `ACTION` step → `["handler","autonomy"]`. Applied to the shipped AT-2: `compliance` (`rule_gate`, EVALUATE) → **`[]` (fully blind)**; `source` (`scored_rule`, ACTION) → `["handler","autonomy"]` (both **already filled**: `handler: emergency_source`, `autonomy: auto`); `approve` (`doa_tier`, ACTION) → `["handler","autonomy"]` (both **already filled**: `handler: request_approval`, `autonomy: gated`). **Net: none of the three AT-2 gates owes any AT-2 *content* obligation today** → an empty-DOA / no-criteria / no-compliance-rule AT-2 is judged governance-complete and run-loadable. The *new* D5 obligations are what newly trip the migration (finding 4).
3. **OQ-B confirmed — the AT-2 control VALUES do not exist in the spec.** `verticals/procurement/procedures.yaml` `emergency_sourcing_round` carries its AT-2 content as **descriptive prose only**: `source` facet note `"on-contract default; RFQ→AVL only as a logged exception"` (`:115` — no criteria/weights); `compliance` facet note `"per-criterion rule gate — AVL · tax · cert · sanctions · single_source"` (`:137` — no predicates); `approve` facet note `"DOA tier band (฿) — escalates the approver above the band"` (`:161` — **no ฿ numbers at all**). Typing the D2 content therefore requires **AUTHORING those values for the first time** — it is *not* a mechanical prose→typed transcription, and ADR-0025's "human-reviewed against the source" check has **nothing numeric to verify against** (OQ-B re-scopes that check).
4. **The migration trap is real and bounded to four net-new obligations.** Once D5 lands (a `scored_rule` action owes a `ScoredRule`; a `rule_gate` evaluate owes ≥1 `ComplianceRule`; a `doa_tier` action owes a `DoaLadder`; a `doa_tier`-bearing procedure owes a `separation_of_duties`), `emergency_sourcing_round` fails `validate_governance_complete` until **exactly four** values are authored: `source.governance_content[ScoredRule]`, `compliance.governance_content[ComplianceGate≥1 rule]`, `approve.governance_content[DoaLadder]`, and `emergency_sourcing_round.separation_of_duties`. → the D5 extension + the migration MUST land in **ONE PR** behind a green golden test (ADR-0025 IN-1).
5. **N=1 confirmed — no second AT-2.** A repo-wide grep for `doa_tier|scored_rule|rule_gate` across `verticals/` matches **only** `verticals/procurement/procedures.yaml`. The vertical's other procedure, `low_stock_reorder_round`, is calm-path AT-3 (`[none, in_file_band, none]`) — **no AT-2 gate kinds → no change needed** (OQ-C iii confirmed). The D7 generator-deferral + the instance-scoped D2 types (provisional-until-N≥2) stand.
6. **OQ-D confirmed — the prose-lint is generated-prose-only by design and would block the shipped AT-2.** `prose_lint.py` docstring (`:14–17`): "It is **NOT** run over hand-authored specs — a shipped `facet.governance` note may legitimately say 'HUMAN approves', which this would flag." The shipped AT-2's hand-authored free-text is full of exactly the flagged classes: decision verbs (`source` `"SELECTION = a scored RULE"` `:119`; `compliance` `"blocks the PO on ANY failed criterion"` `:141`; `approve` `"HUMAN approves"` / `"escalates the approver"` `:161/:165`) and handler identifiers (`emergency_source`, `request_approval` — caught by `_SNAKE_CASE`/the registered-handler scan). A blanket application **blocks the shipped, correct procedure** → a scoped variant is required (OQ-D). The lint's internals help: `_scan` already separates `_VALUE_SCANS` from `_VERB_SCANS` (`:132–148`), so a value-only variant is a clean call.
7. **OQ-E confirmed — no per-spec `schema_version` needed in v1.** Specs re-load from `verticals/<name>/procedures.yaml` at runtime on every `load_procedures` call (`spec.py:439–449`). `PipelineRun` (`runs.py:54`) persists `procedure_id` (Text, `:60`) — **not a spec snapshot** — so a resume/replay re-loads the *current* YAML. A vertical-file `version: int | None` already exists (`spec.py:384` `VerticalProcedures.version`; `procedures.yaml:29` `version: 0`) if coarse file-level versioning is ever wanted. (Latent edge, out of v1 scope: a resume after a spec edit re-loads the changed spec.)
8. **The substrate the build edits is located + confirmed net-shape.** D2 lands in `spec.py` (`GateKind` 6 kinds `:91`; `DecisionCondition` point-at precedent `:156`; `extra="forbid"` throughout; `_validate_step` `:275`). D4/D5 land in `draft.py` (`GOVERNANCE_FIELDS` `:55`; `derive_governance_todo` `:201`; `lift_to_step` injects absent stubs `:136`; `StepDraft`/`ProcedureDraft` the restricted types `:62/:106`) + `orchestrator.py` (`validate_governance_complete` `:168` ← `validate_runnable` `:114`; the `StepExecutor` Protocol `:91` + `run_procedure`/`execute_steps`/`_suspends` — the per-kind executor seam OQ-A's run path would extend). Test homes (exact, verified): `tests/services/engine/procedures/test_spec.py` (D2/D3 validators), `…/test_draft_lift_governance.py` (D4 recursive disjointness + D5 obligation derivation + the negative regression), `…/test_orchestrator.py` (the gate + any run executors), `…/test_prose_lint.py` (the scoped variant), `…/test_example_procedures.py` (the golden test — loads every shipped vertical), `tests/api/test_procedure_draft_routes.py` (recorded-fixture route tests, zero host-state).

## LOCKED (from ADR-0025 — render faithfully; do NOT re-litigate)

Each maps to an Accepted ADR-0025 decision; built to, not reopened. If the fact-pack ever showed a LOCKED item *must* change to build it, that is an ADR-0025 amendment (G-gated) — **STOP and surface**, never bake a deviation from an Accepted ADR into the PLAN.

- **LOCKED-1 (D1)** — scope is the managerial **LAYER**, not the generator: author + (per OQ-A) run + demo on the **hand-authored** AT-2. Does **not** extend the classify-generator to emit AT-2 (the abstain path stays — PLAN-0041 LOCKED).
- **LOCKED-2 (D2)** — ONE discriminated `Step.governance_content` union keyed to `gate_kind` (`AT2Governance = DoaLadder | ScoredRule | ComplianceGate`, `Field(discriminator="kind")`) + `Procedure.separation_of_duties: list[SoDConstraint]`. **Authoritative top-level fields** (the source of truth, like `Step.threshold`), **never** inside the non-authoritative `facet` (D2-A4); `gate_kind` **points at** the content (D2-A3). **Money is `Decimal`, never `float`.** Instance-scoped + **provisional-until-N≥2** (docstrings); genericization gated on the D7 re-trigger. NOT four bare `Optional`s (Alternative 3 rejected); NOT in the facet (Alternative 2 rejected).
- **LOCKED-3 (D3)** — **bypass is UNREPRESENTABLE, not defaulted-false.** No `skips_gate` / `blocks_po=False` flag (Alternative 4 rejected). The waiver (`EmergencyWaiverPolicy`) carries no skip — it only relaxes a closed `RelaxableConstraint` enum (which cannot name compliance/SoD), `escalate_to` a strictly-higher authority, `requires_justification: Literal[True]`. Compliance always blocks (`ComplianceRule.blocks_po: Literal[True]`). The DOA ladder is total + unambiguous (single currency; `min_amount` strictly increasing; first tier `min_amount == 0`; top tier unbounded; non-empty; no `approver_role == resolved requester role`).
- **LOCKED-4 (D4)** — **governed ≠ generated holds at the AT-2 authoring surface.** `governance_content` + `separation_of_duties` are **H** (human-author-only): added to `GOVERNANCE_FIELDS`, **never** declared on `StepDraft` / `ProcedureDraft`, injected by the lift as **absent** stubs. Recursive draft-disjointness (no `DoaTier`/`ScoredRule`/`ComplianceRule`/union variant reachable from any draft type) + a `StepFacet`-unreachability CI check (no AT-2 content type reachable from `StepFacet.model_fields`; the facet may only carry the `gate_kind`, never embed a ladder). The prose-lint extends over the AT-2 free-text surfaces (scope per OQ-D).
- **LOCKED-5 (D5)** — the run-gate becomes AT-2-aware: **SEMANTIC** completeness, not presence. A `scored_rule` action owes a `ScoredRule`; a `rule_gate` evaluate owes ≥1 `ComplianceRule`; a `doa_tier` action owes a `DoaLadder`; a `doa_tier`-bearing procedure owes a `separation_of_duties`. An empty/duplicate/unreachable/`∞`-floored ladder, a zero-criteria scored rule, or a no-rules compliance gate must each **fail**. **SoD completeness is enforcement-time-scoped (concretized to the OQ-A=A1 ratification — see lineage note):** at **author time (v1, A1)** the SoD obligation is **structural + role-level** — the constraint must exist, reference **≥2 distinct steps**, and carry no `approver_role` equal to the resolved **requester role** (a role-label / structural check; a missing / single-step / degenerate SoD, or `approver_role == requester_role`, **fails**). **Principal-level SoD** (resolved **principal identity**, not role labels — Alternative 5 rejected; **fail closed** if either side is unresolvable to a distinct human) is a **run-time** check and moves under the **deferred A2 run path (AC-13-ALT)**. The **negative regression test (a hollow-but-complete AT-2 skeleton MUST be refused) is the D5 ratification gate.**
  > *Lineage (read as `superseded by the A1 ratification`, NOT a contradiction or amendment of ADR-0025 D5):* D5's "an SoD whose roles resolve to one principal must fail" is inherently a **run-time** statement; ADR-0025 D6 / OQ-3 left the concrete v1 run-vs-author call to this PLAN, and Cray ratified **A1 (author + render)** — so principal resolution is deferred to A2 (the engine has **no** principal-identity layer today: fact-pack — no `principal`/`identity`/`requester` model in `spec.py`/`orchestrator.py`; `Agent` is the machine actor). This is a **PLAN-level concretization of the v1 boundary**; the principal-level rule is **preserved**, relocated to the A2 path (AC-13-ALT), not removed.
- **LOCKED-6 (D6 / OQ-A)** — IF the run path ships, the **four hard guarantees are non-negotiable**: (1) principal-level SoD at run (fail closed on alias-collapse / unresolvable identity); (2) audit-to-control as an engine side-effect (not an authorable step; `autonomy: auto` forbidden downstream of a gate); (3) render/route/block-only (no ERP I/O, no PO emission; the ADR-007 approve→execute gate stays the only write path); (4) no live LLM/generation in the v1 run path. **If any guarantee cannot be met in v1, author + render only is the correct ship.**
- **LOCKED-7 (D7)** — AT-2 stays N=1; the **generator stays deferred**. A CI assertion counts AT-2-class procedures across `verticals/*/procedures.yaml`; at **N ≥ 2** it flags "re-evaluate the AT-2 generator deferral (ADR-0025 D7)". Genericizing the D2 types from instance-scoped to reusable is gated on the same N ≥ 2. This PLAN does **not** genericize the types and does **not** make the classify generator emit AT-2.
- **LOCKED-8 (D8)** — the offline oracle = the three red-team fixtures (hollow-but-complete; leak-in-free-text; identity-collapse + un-gated-audit) + the unit suite, LLM **stubbed**. No live MS-S1 (generation is out of scope; CLAUDE.md §8).

## Acceptance Criteria

Grouped by build step. The **offline oracle is the merge gate**; there is **no live run** in this PLAN (generation out of scope; OQ-A **ratified = A1**, author+render). Per CLAUDE.md §8 the pass/fail read for the negative hollow-but-complete regression (AC-9) is **pre-committed before the test is written** (it is the D5 ratification gate).

### Step 1 — the typed content home + D3 validators + the D4 type-coupled invariants (D2/D3/D4)

- [ ] **AC-1 — the discriminated `Step.governance_content` union (D2 / LOCKED-2).** In `spec.py`: `DoaTier{min_amount: Decimal, approver_role: RoleId}`, `DoaLadder{kind: Literal["doa_tier"], tiers: list[DoaTier], emergency_waiver: EmergencyWaiverPolicy}`, `ScoredRule{kind: Literal["scored_rule"], criteria: list[ScoredCriterion{name, weight: Decimal}], default_source: SourcePolicy, exception_policy: ExceptionPolicy}`, `ComplianceGate{kind: Literal["rule_gate"], rules: list[ComplianceRule{criterion: ComplianceCriterion(StrEnum), spec: Predicate, blocks_po: Literal[True]}]}`, with `AT2Governance = Annotated[DoaLadder|ScoredRule|ComplianceGate, Field(discriminator="kind")]` and `Step.governance_content: AT2Governance | None = None`. `extra="forbid"`, `Field(description=...)` on every field, money `Decimal`. **Does NOT add a second top-level `Step.tiers`** (finding 1 — the DOA `tiers` nest inside `DoaLadder`).
- [ ] **AC-2 — `Procedure.separation_of_duties` (D2 / LOCKED-2).** `SoDConstraint{distinct_steps: frozenset[StepId] (min 2)}`; `Procedure.separation_of_duties: list[SoDConstraint] = []`; a `model_validator` rejects dangling `step_id` references and degenerate single-step sets.
- [ ] **AC-3 — bypass is unrepresentable (D3 / LOCKED-3).** `EmergencyWaiverPolicy{relaxes: list[RelaxableConstraint] (closed enum, non-empty; cannot name compliance/SoD), escalate_to: RoleId, requires_justification: Literal[True]=True}` — no skip/lower/waive field exists. `ComplianceRule.blocks_po: Literal[True]`. A `DoaLadder` `model_validator` enforces: single currency; `min_amount` strictly increasing (no overlap/equal); first tier `min_amount == 0`; non-empty; top tier unbounded; no `approver_role` equal to a resolved requester role (author-time + re-checked at the gate). A waiver `model_validator` enforces strict escalation (`escalate_to` higher than every tier it relaxes). Unit-tested on positive + negative cases in `test_spec.py`.
- [ ] **AC-4 — the new fields are H, off every draft type (D4 / LOCKED-4).** `governance_content` + `separation_of_duties` added to `GOVERNANCE_FIELDS` (`draft.py`); **never** declared on `StepDraft`/`ProcedureDraft`; `lift_to_step`/`lift_to_procedure` inject them as **absent** stubs; the lifted result still round-trips `load_procedures`.
- [ ] **AC-5 — recursive disjointness + `StepFacet`-unreachability CI checks (D4 / LOCKED-4).** A test asserting no `DoaTier`/`ScoredRule`/`ComplianceRule`/`DoaLadder`/`ComplianceGate`/`SoDConstraint` (nor any union variant) is reachable from any draft type's fields (extends the AC-A4 disjointness test to **recursive**), **and** no AT-2 content type is reachable from `StepFacet.model_fields`. Adding an AT-2 content type to a draft type or to the facet later **fails CI**.
- [ ] **AC-6 — additive: every shipped vertical still loads + passes the *current* gate after Step 1 (no-regression).** Step 1 adds only optional fields; with the gate unchanged, all 5 shipped procedures (incl. the prose-only AT-2) still pass `validate_governance_complete`. (The gate change is Step 2.)

### Step 2 — the AT-2-aware run-gate + the procurement migration, in ONE PR behind a green golden test (D5 / migration trap)

- [ ] **AC-7 — `derive_governance_todo` extended to semantic AT-2 completeness (D5 / LOCKED-5).** A `scored_rule` action owes a non-empty `ScoredRule`; a `rule_gate` evaluate owes a `ComplianceGate` with ≥1 `ComplianceRule`; a `doa_tier` action owes a valid `DoaLadder`; a `doa_tier`-bearing procedure owes ≥1 `separation_of_duties`. Completeness is **semantic** (empty/duplicate/unreachable/`∞`-floored ladder, zero-criteria scored rule, no-rules compliance gate, and a **single-step / `approver_role == requester_role` SoD** all count as **unfilled/invalid**), not mere presence. `validate_governance_complete` **re-derives** (never trusts a stored worklist) and raises on the first unfilled/invalid AT-2 obligation. **SoD at this author-time gate is structural + role-level** (the constraint exists, references ≥2 distinct steps, and carries no `approver_role` equal to the resolved requester role). The **principal-identity** SoD (distinct resolved humans; fail-closed on alias-collapse / unresolvable identity) is a **run-time** check that moves to the **deferred A2 path (AC-13-ALT)** — OQ-A=A1 (author+render) defers principal resolution and the engine has no principal-identity layer today (fact-pack: no `principal`/`identity`/`requester` model in `spec.py`/`orchestrator.py`; `Agent` is the machine actor).
- [ ] **AC-8 — the procurement AT-2 migrated to typed content in the SAME PR (finding 4).** `emergency_sourcing_round` gains exactly the four net-new values: `source.governance_content` (a `ScoredRule`), `compliance.governance_content` (a `ComplianceGate`, ≥1 rule), `approve.governance_content` (a `DoaLadder` + waiver), and `separation_of_duties` (requester step ≠ approver step). The values are **human-authored** (OQ-B) and Cray-signed-off at ratification (re-scoped from "human-reviewed against source" — finding 3). `low_stock_reorder_round` is **unchanged** (finding 5).
- [ ] **AC-9 — the negative hollow-but-complete regression (the D5 ratification gate / D8 fixture 1 / LOCKED-5).** A hollow-but-complete AT-2 skeleton (empty/duplicate/`∞` DOA tiers, zero scored criteria, zero compliance rules, single-step SoD or `approver_role == requester_role`) **MUST be refused** by `validate_governance_complete`. (Under OQ-A=A1 the author-time testable collapse is the **role-level** form `approver_role == requester_role` / a single-step SoD; the **principal-level** collapse — roles resolving to one human — is a run-time A2 test under AC-13-ALT.) The pass/fail read is **pre-committed before the test is written**.
- [ ] **AC-10 — the green golden test across the diff (ADR-0025 IN-1).** A test that loads **every** shipped vertical through `validate_governance_complete` across the PR diff: the non-AT-2 procedures still pass; `emergency_sourcing_round` now passes **because it is typed** (not because the gate is blind). Proves the migration trap is closed atomically in one PR. Home: `test_example_procedures.py`.

### Step 3 — the scoped prose-lint over AT-2 free-text + provenance banding (D4 / OQ-D)

- [ ] **AC-11 — a scoped prose-lint variant for hand-authored AT-2 free-text (D4 / OQ-D).** A variant that runs the **value classes only** (`_VALUE_SCANS`: currency / magnitude / percent / decimal / numeric / spelled-number) + an approver-role-token check over the AT-2 free-text surfaces (waiver justification, tier/criterion `note`/`label`, `description`, `goal`), and **omits** the decision-verb / approval-phrase classes (`_VERB_SCANS`) and the broad `_SNAKE_CASE`/`_CAMEL_CASE` identifier catch — both of which legitimately appear in hand-authored governance notes (finding 6). A ฿-amount / weight / role token in any AT-2 free-text field **blocks load**; "HUMAN approves" / "blocks the PO" / a registered handler name does **not**. Unit-tested positive + negative in `test_prose_lint.py`, including the shipped AT-2's real notes (must pass).
- [ ] **AC-12 — provenance banding on the AT-2 free-text (D4 / reuses PLAN-0039).** The AT-2 free-text fields render with a provenance band marking them non-authoritative. Label: **"ADVISORY — NOT A CONTROL"** / "NON-AUTHORITATIVE" (the ADR's "GENERATED — NOT A CONTROL" wording is misleading on hand-authored text — OQ-D). Reuses the PLAN-0039 read-only viewer component; no second renderer.

### Step 4 — the v1 run boundary (OQ-A — Cray sets the boundary)

- [ ] **AC-13 — (RATIFIED OQ-A=A1: author + render only — the v1 path) the gate is *visible* (D6 fallback / OQ-A).** The typed AT-2 content + the read-only render (Step 3) make the DOA ladder / compliance rules / SoD **visible** in the viewer (read-only, reusing PLAN-0039) — the Box-3 "the gate is no longer blind" story — **without** run-time enforcement. The four D6 guarantees are not all met in v1 (guarantee #1 principal-level SoD needs a principal-identity layer the engine lacks — see OQ-A), so D6 mandates the conservative author+render ship. **This is the active v1 acceptance criterion for Step 4.**
- [ ] **AC-13-ALT — DEFERRED to a follow-on PLAN (NOT a v1 acceptance criterion).** With OQ-A **ratified = A1**, the A2 run path is **out of v1**. Retained here as the home for the deferred work: the **principal-identity SoD run-check** (Delta-2 — distinct resolved humans; fail-closed on alias-collapse / unresolvable identity) **plus** the deterministic per-kind enforcement executors extending the shipped orchestrator (`StepExecutor` seam): `doa_tier` computes the tier from the `Decimal` amount + ladder and routes/suspends to the approver; `rule_gate` blocks the PO on any failed criterion; `scored_rule` selects on the typed rule (LLM only summarises); audit-to-control as an engine side-effect; render/route/block-only (no ERP I/O); the ADR-007 approve→execute gate stays the only write path. **Gated on a principal-identity-resolution capability that does not exist in the procedures engine today** (fact-pack: no `principal`/`identity`/`requester` model in `spec.py`/`orchestrator.py`). Built only when that capability lands, as a **separate PLAN**.

### Step 5 — the offline oracle (D8 / LOCKED-8)

- [ ] **AC-14 — the three red-team fixtures (D8 / LOCKED-8).** (1) hollow-but-complete skeleton → `validate_governance_complete` rejects (= AC-9); (2) leak-in-free-text corpus (a ฿-amount, a weight, an approver-role token seeded into the waiver justification, a tier `note`, a criterion `label`) → the scoped prose-lint (AC-11) blocks load + the fields are typed non-authoritative; (3) identity-collapse + un-gated-audit — **under A1 (v1)** the author-time testable form is the **role-level** collapse (`approver_role == requester_role` / a single-step SoD), plus an audit step authored `auto`/absent and a downstream `auto` step → **refused at the gate** (author+render). The **principal-level** collapse (SoD roles resolving to one human) is the **A2 run-test** under the deferred AC-13-ALT (if it ever ships, the run emits no "governed" verdict). **Fails closed** either way. The LLM is **stubbed** (deterministic, offline).
- [ ] **AC-15 — the offline gate is green (the merge bar).** `ruff` + `ruff-format` + `mypy --strict services/` clean; the full offline suite passes (AC-1..AC-14); no live MS-S1.

## Out of Scope

- ❌ **The AT-2 generator** (D7 — Rule-of-Three-deferred until N≥2; the CI re-trigger is IN as a *guard*, the generator itself is OUT).
- ❌ Making the **classify generator emit AT-2** (the abstain path stays — PLAN-0041 LOCKED).
- ❌ **Genericizing the D2 types** beyond the observed `emergency_sourcing_round` signature (gated on the D7 N≥2 re-trigger).
- ❌ **Live ERP / email I/O** behind the write gate (the run path, if any, is render/route/block-only — D6; the ADR-007 approve→execute gate is untouched).
- ❌ **Any live LLM / MS-S1 run** (the oracle is offline; generation is out of scope — if a live run is ever proposed it is host-state → Cray go, CLAUDE.md §8).
- ❌ The **Box-4 economic-impact facet** (O-2 / Rock 3, after N≥3); agent-interop (O-4).
- ❌ A **per-spec `schema_version`** (finding 7 — specs re-load from YAML; not needed in v1).
- ❌ Editing the constitutional / convention / lesson files (Code amends per ADR-009 D2 follow-on TODOs).

## Surfaced Open Questions for Cray — RATIFIED (Cray, session 85)

ADR-0025 left these residual **build-level** forks to this PLAN. Each was presented with options + a recommendation grounded in the fact-pack and is now **Cray-ratified** (none silently resolved — Tier-1 rule #8/#12). **Ratification (session 85): OQ-A = A1, OQ-B = B2, OQ-C / OQ-D / OQ-E = confirmed-as-recommended.** The options + grounding are retained below for the audit trail; the **chosen** option is stamped at the head of each.

- **OQ-A — THE crux: the concrete v1 run-vs-author boundary (implements OQ-3 / D6). — RATIFIED: A1 (Cray, session 85).** ADR-0025 D6 ratified the *boundary* (enforce deterministically under the four guarantees, **else** author + render) but deferred the **concrete v1 call** to this PLAN "per the four-guarantee feasibility."
  - *Option A1 (recommended):* **author + render only in v1.** Type the content (Step 1), close the D5 gate (Step 2), render the gate visible read-only (Step 3) — the gate is *seen* and *enforced at author-time*, not at run. **Grounding:** D6 guarantee #1 (principal-level SoD, fail closed on alias-collapse / unresolvable identity) requires resolving a human *requester principal* and *approver principal* to **distinct** humans — but the procedures engine has **no principal/identity-resolution layer today** (`Agent` models the *machine* actor — `agent_id` / `llm_model` / `allowed`; there is no human requester/approver identity model in `spec.py`/`orchestrator.py`). With guarantee #1 not demonstrably met in v1, **D6 itself says fall back to author + render.** This also matches Code's s84 lean (recorded in the closeout): strictly safer; a read-only artifact never certifies an un-owned control as owned (red-team §3); and it **closes the live defect regardless** (the D5 gate-awareness is independent of run). Demo: A1 already proves "the gate is no longer blind" + renders the DOA ladder / compliance / SoD visibly — a strong Box-3 "Action = contract" artifact.
  - *Option A2:* **run under the four D6 guarantees** (AC-13-ALT). Stronger Box-3 story (the gate actually routes/blocks at run) — **but only if all four guarantees demonstrably hold in v1**, which requires building the net-new principal-identity-resolution capability first (a non-trivial addition, and the exact place a bug raises blast radius from "wrong document" to "wrong procurement action" — ADR-0025 Consequences).
  - **Recommendation: A1**, with A2 as a **follow-on PLAN once a principal-resolution capability exists.** The demo-strength delta (A2 routes/blocks at run; A1 shows the gate is no longer blind + renders the contract) does not justify shipping a run path that cannot guarantee principal-level SoD. **Cray sets the boundary — this is the load-bearing call and it sizes the PLAN** (A1 = Steps 1–3+5; A2 adds Step 4's AC-13-ALT + the principal layer).
- **OQ-B — the value-authoring gap (load-bearing for the migration; finding 3). — RATIFIED: B2 (Cray, session 85; B1 folds in if Cray supplies real Fastenal-class numbers at ratification).** D2's typed home requires **authoring** the procurement AT-2's control VALUES for the first time — there are no ฿ DOA tiers, no scored-rule criteria/weights, no compliance predicates in the spec — so ADR-0025's "human-reviewed against the source" check has nothing numeric to verify against.
  - *Option B1:* **Cray (the design partner) supplies the real Fastenal-class tiers / criteria / predicates** now. The controls become real + demo-credible, but the PLAN cannot land until Cray provides them.
  - *Option B2 (recommended):* **author plausible, clearly-marked placeholder control values** that satisfy the typed shape + the D3 validators (strict-monotonic total-cover ฿ ladder with first tier `min_amount == 0`; ≥2 scored criteria with weights; ≥1 compliance predicate; requester≠approver SoD), flagged **provisional-until-Cray-confirms** (mirrors the O-1 "conservative floor" stance). Unblocks the build; the **shape + gate are real**, the **numbers are a labelled floor.** Because the values are H (human-authored), **Cray IS the author** — so the "human-reviewed against source" gate is re-scoped to **"human-authored + Cray-signed-off"**, satisfied at ratification.
  - **Recommendation: B2** (with B1 folded in if Cray can supply real numbers at ratification). Either way: **no silent invention of authority-bearing numbers** — the placeholder values are explicitly labelled provisional and Cray signs off the actual figures. **Cray adjudicates.**
- **OQ-C — the golden test + migration sequencing (the migration trap; findings 4/5).** *Recommendation:* (i) the **golden test** = `test_example_procedures.py` loads every shipped vertical through `validate_governance_complete` across the diff (non-AT-2 still pass; AT-2 now passes *because* typed); (ii) the **negative regression** (AC-9) = a hollow-but-complete AT-2 skeleton MUST be refused (pass/fail read pre-committed before the test is written); (iii) `low_stock_reorder_round` needs **no change** (no AT-2 gate kinds — finding 5); (iv) the D5 gate extension + the procurement-AT-2 typing land in **ONE PR** (Step 2). **RATIFIED: confirmed-as-recommended (Cray, session 85)** (essentially LOCKED by ADR-0025 IN-1).
- **OQ-D — the prose-lint extension surface (the scope clash; finding 6).** A blanket `prose_lint` over the hand-authored AT-2 free-text would block the shipped, correct procedure (its notes legitimately say "HUMAN approves" / "blocks the PO" and name handlers). *Recommendation:* the **scoped variant** of AC-11 (value classes only; omit the decision-verb / approval-phrase classes + the broad identifier catch), plus the provenance band **"ADVISORY — NOT A CONTROL"** / "NON-AUTHORITATIVE" (not the ADR's "GENERATED — NOT A CONTROL", misleading on hand-authored text). **RATIFIED: confirmed-as-recommended (Cray, session 85).**
- **OQ-E — `schema_version` for persisted specs (ADR-0025 IN-1 "consider"; finding 7).** *Recommendation:* **no per-spec `schema_version` in v1** — specs re-load from YAML at runtime; `PipelineRun` persists `procedure_id`, not a spec snapshot; the existing vertical-file `version` field covers coarse versioning if ever wanted. (Latent edge noted: resume-after-spec-edit re-loads the changed spec — out of v1 scope.) **RATIFIED: confirmed-as-recommended (Cray, session 85).**

## Steps

Build order respects the migration trap (the D5 gate extension + the procurement typing land together in Step 2). The D2 types + D4 type-coupled invariants land first (Step 1, additive — the shipped AT-2 still passes because the gate does not yet require content).

### Step 1 — typed content home + D3 validators + D4 type-coupled invariants (AC-1..AC-6)
- `spec.py`: add the discriminated `Step.governance_content` union (`DoaLadder`/`ScoredRule`/`ComplianceGate` + their nested models) keyed to `gate_kind`, and `Procedure.separation_of_duties`, with the D3 `model_validator`s (Decimal money; total-cover strict-monotonic ladder; unrepresentable bypass; strict-escalation waiver; non-waivable compliance/SoD; SoD ref-integrity). **Do NOT add a top-level `Step.tiers`** (finding 1 — the DOA `tiers` nest in `DoaLadder`).
- `draft.py`: register `governance_content` + `separation_of_duties` in `GOVERNANCE_FIELDS`; keep them off `StepDraft`/`ProcedureDraft`; have `lift_to_step`/`lift_to_procedure` inject them as absent stubs.
- Tests (`test_spec.py`, `test_draft_lift_governance.py`): the D3 validator positive/negative cases; the recursive-disjointness + `StepFacet`-unreachability CI checks (AC-5); the no-regression load of all 5 verticals under the *current* gate (AC-6).

### Step 2 — the AT-2-aware gate + the procurement migration, ONE PR, green golden test (AC-7..AC-10)
- Pre-commit the pass/fail read for the negative hollow-but-complete regression (AC-9) **before** writing the test (the D5 ratification gate).
- `draft.py`/`orchestrator.py`: extend `derive_governance_todo` (and so `validate_governance_complete`) to the four semantic AT-2 obligations (AC-7); **SoD at this author-time gate is structural + role-level** (≥2 distinct steps; no `approver_role == resolved requester role`) — the principal-identity SoD run-check is deferred to the A2 path (AC-13-ALT, OQ-A=A1).
- `verticals/procurement/procedures.yaml`: author the four net-new typed values on `emergency_sourcing_round` (OQ-B values; finding 4) — **in the same PR** as the gate extension.
- Tests: the negative regression (AC-9); the golden test across the diff (AC-10, `test_example_procedures.py`); `test_orchestrator.py` gate cases.

### Step 3 — scoped prose-lint + provenance banding (AC-11/AC-12)
- `prose_lint.py`: a scoped variant (value classes only; omit verb/approval + broad identifier classes) for hand-authored AT-2 free-text; unit-tested including the shipped AT-2's real notes (must pass).
- The PLAN-0039 viewer: render the AT-2 free-text with the "ADVISORY — NOT A CONTROL" provenance band (reuse the component; no second renderer).

### Step 4 — the v1 run boundary (OQ-A **ratified = A1**; AC-13 is the path)
- **OQ-A = A1 (RATIFIED, Cray s85):** **author + render only** — the gate is visible read-only (Step 3 already delivers the render); **no run executors in v1**. Step 4 adds no new build beyond the Step-3 render, so the **v1 build surface is Steps 1–3 + 5** (AC-13 is the Step-4 acceptance criterion).
- **AC-13-ALT (the A2 run path) is DEFERRED to a follow-on PLAN** — built only when a principal-identity-resolution capability lands (it does not exist in the engine today). It is the home for the deterministic per-kind enforcement executors under the four D6 guarantees (extending the `StepExecutor` seam, not a new engine; the ADR-007 write gate stays the only write path) **and** the Delta-2 principal-level SoD run-check. **Not part of v1.**

### Step 5 — the offline oracle (AC-14/AC-15)
- The three D8 red-team fixtures (LLM stubbed) + the full offline suite green; `ruff`/`ruff-format`/`mypy --strict services/` clean; no live MS-S1.

## Verification

Mapped to the gate-vs-evidence split (CLAUDE.md §8). Because OQ-A is **ratified = A1** (author+render) and generation is out of scope, **the gate is fully offline and there is no live run in this PLAN.**

- **Gate (CI / offline) — the binding bar:** the D3 validator suite (AC-3); the recursive-disjointness + `StepFacet`-unreachability checks (AC-5); the AT-2-aware gate refusing the hollow-but-complete skeleton (AC-9, pass/fail pre-committed) and accepting the typed procurement AT-2 (AC-10 golden test); the scoped prose-lint passing the shipped notes + blocking seeded leaks (AC-11/AC-14-2); the three red-team fixtures green (AC-14); `ruff` + `ruff-format` + `mypy --strict services/` clean (AC-15). **The negative regression (AC-9) is the D5 ratification gate.**
- **No-regression:** all 5 shipped procedures load + pass the gate (the AT-2 *because* typed, not because blind); `low_stock_reorder_round` byte-unchanged (finding 5); `load_procedures` shape behaviour unchanged; the ADR-007 approve→execute write gate untouched; `Step.tiers` (the PLAN-0022 handler taxonomy) unshadowed (finding 1).
- **Guardrail-erosion tripwire:** AC-5 — if anyone later adds an AT-2 content type to a draft type or to `StepFacet`, CI fails (the mechanical guarantee, not a hoped-for behaviour); the D7 N≥2 re-trigger assertion fires when a second AT-2 lands.
- **Evidence (NONE live in v1):** no live MS-S1 run is expected. The demo artifact = the typed procurement AT-2 rendered in the gate (the DOA ladder / compliance rules / SoD visible, with the "ADVISORY — NOT A CONTROL" band on the free-text) — the Box-3 "Action = contract" scene, offline. If Cray ever sets OQ-A = A2, any live enforcement run is host-state → Cray go (CLAUDE.md §8).
- **STOP-and-surface:** if building any LOCKED item reveals an Accepted ADR-0025 decision must change, that is an ADR-0025 amendment (G-gated) — surface to Cray, do not bake a deviation into the build.

---

*PLAN-0042 drafted (uncommitted) by Cowork (session 85), Tier-1 governance authoring (ADR-009 D1 interim per ADR-013 phased relocation). Its shape is LOCKED by ADR-0025 D1–D8 (Accepted #463) — a focused **build/execution** PLAN, **no new ADR**; it renders ADR-0025's Implementation Notes and owns the migration sequencing OQ-5 assigned it. The OQ-A…E forks are the residual build-level calls ADR-0025 left to this PLAN, surfaced not silently resolved (Tier-1 rule #8/#12); finding 1 (the `Step.tiers` naming collision) is a fact-pack divergence the dispatch did not name, flagged per Tier-1 rule #4. **OQ-A=A1 and OQ-B=B2 are now Cray-ratified (session 85); OQ-C/D/E confirmed** — the load-bearing forks are closed. This s85 revision applied three surgical deltas (record the ratification; fold the Code-R2 SoD principal-vs-role scoping finding under A1; mark AC-13-ALT [the A2 run path] deferred to a follow-on PLAN) — **not a re-draft**; the PLAN spine is unchanged. The Delta-2 finding originated with Code's independent R2 review (ADR-012 D4.3 author≠reviewer). Code R2-reviews the delta + commits via a `docs/*` PR (ADR-009 D2). Cowork does not git (G5). AI-assisted (Cowork drafter); no `Co-Authored-By` per CLAUDE.md §7.*
