# Alembic configuration for vero-lite.
# The runtime database URL is injected by alembic/env.py from
# services.api.config.settings (DATABASE_URL / .env) — not set here.

[alembic]
script_location = alembic
prepend_sys_path = .
